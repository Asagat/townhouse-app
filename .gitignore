# Python
__pycache__/
*.py[cod]
*.class
venv/
.venv/
venv~/
.pytest_cache/

# Environment
.env
.flaskenv
.env.local
.env.*.local
# Но шаблон-пример — коммитим в репозиторий:
!.env.example

# Databases (Local)
*.sqlite3
*.db

# Дампы БД (содержат данные — в репозиторий не коммитим)
# Локальный дамп обычно лежит в корне проекта как townhouse_db.sql.gz.
*.sql.gz
townhouse_db.sql.gz

# OS
.DS_Store
.directory

# IDEs
.vscode/
.idea/
.zed/

# Справочные шаблоны (не для прода/репо)
/templates/
