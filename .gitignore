# Python
__pycache__/
*.py[cod]
*.class
venv/
.venv/
venv~/
.pytest_cache/

# Environment
.env
.flaskenv
.env.local
.env.*.local
# Но шаблон-пример — коммитим в репозиторий:
!.env.example

# Databases (Local)
*.sqlite3
*.db

# Дампы БД (содержат данные — в репозиторий не коммитим)
# Локальный дамп обычно лежит в корне проекта как townhouse_db.sql.gz.
*.sql.gz
townhouse_db.sql.gz
# Датампы-снимки со значениями (папка бэкапов + рабочие .sql-снимки в backend).
backend/backups/
backend/dev_backup_*.sql
backend/backup_*.sql
backend/prod_backup_*.sql
backend/prod_users_backup_*.sql
# Снапшоты-дапмпы данных для разовых накатов (генерируются локально, из репозитория не нужны)
backend/migrations/output/*.sql
*.dump

# OS
.DS_Store
.directory

# IDEs
.vscode/
.idea/
.zed/

# ------------- templates (примеры/генерация vs данные-переноса) -------------
# Не коммитим: генер. выхлоп подготовки, авто-копии, примеры печати, исходный Access.
/templates/clean/
/templates/Миграция_ЧИСТО.xlsx
/templates/*.pdf
/templates/Учет_коммунальных_be.accdb

/templates/*
# Файлы данных переноса („Миграция данных FTH*.xlsx/.ods“) с 09.2026 в git НЕ хранятся:
# синхронизируются между ПК напрямую через Synology Drive (см. DEPLOY.md §7.3-7.4).
# Тест сверки (backend/tests/test_control_sums_vs_source.py) сам пропускается, если файла нет.
