# Структура проекта Townhouse

## Обзор
Проект представляет собой SPA (Single Page Application) для управления жилищно-коммунальным хозяйством. Разработан на React с использованием Refine framework.

**Архитектура учёта показаний и начислений построена по принципу "документ → регистр":**
- **Документы** (`meter_reading_documents`, `accrual_documents`) — это шапки операций: массовый ввод показаний или начисление за период создаёт документ, который группирует связанные записи и хранит общие атрибуты (название, дата, вид услуги).
- **Регистры** (`meter_readings`, `accruals_register`) — это плоские таблицы фактических записей (конкретное показание конкретного счётчика, конкретное начисление конкретному лицевому счёту), каждая запись опционально ссылается на породивший её документ через `document_id` / `accrual_document_id`.
- Удаление документа каскадно удаляет все связанные записи регистра (`ondelete="CASCADE"`).

## Технологический стек
- **React 18** - библиотека для построения пользовательских интерфейсов
- **Refine** - фреймворк для CRUD-приложений
- **React Router v6** - маршрутизация в приложении
- **Ant Design** - библиотека компонентов UI
- **TypeScript** - типизированный JavaScript
- **Vite** - сборщик и dev-сервер
- **Docker** - контейнеризация приложения
- **dayjs** - работа с датами и временем

---

## 📁 Структура папок и файлов

### Корневая директория репозитория

```
townhouse-app/
├── frontend/src/       # SPA (React + Refine) — см. ниже
├── backend/            # FastAPI + SQLAlchemy + Alembic (модели, роутеры, тесты)
├── scripts/            # dev.sh, setup_vps.sh, deploy_vps.sh, restore_townhouse.sh, …
├── deploy/             # nginx-конфиги (VPS/дом), systemd-юнит townhouse-backend
├── templates/          # файлы-источники миграции данных (Миграция данных FTH.xlsx)
├── .github/            # CI из GitHub Actions (без SSH-деплоя: серверов staging/production нет)
│   └── workflows/
│       ├── ci.yml               # pytest на disposable PostgreSQL 16 + frontend build (push/PR в main)
│       └── docker-build.yml     # сборка образов backend/frontend в ghcr.io (main / тег v*)
└── docker-compose.yml  # локальный стек: postgres/backend/frontend/nginx
```

### Корневая директория: `src/`

```
src/
├── App.tsx                    # Главный компонент, роутинг, провайдеры
├── main.tsx                   # Точка входа, монтирование React
│
├── components/                # Переиспользуемые компоненты
│   ├── common/               # Общие компоненты для всего приложения
│   │   ├── RecordFormModal.tsx      # Модалка создания/редактирования
│   │   ├── ReferenceSelect.tsx      # Выбор из справочников (FK)
│   │   └── renderFieldControl.tsx   # Рендер полей формы
│   │
│   ├── layout/               # Компоненты макета
│   │   ├── Sidebar.tsx               # Главное боковое меню
│   │   ├── SidebarHeader.tsx         # Шапка сайдбара
│   │   ├── SidebarExpandedCategory.tsx # Раскрытая категория
│   │   └── SidebarCollapsedCategory.tsx # Свернутая категория
│   │
│   ├── meter-readings/       # Компоненты для показаний счетчиков
│   │   └── BulkReadingsModal.tsx     # Массовый ввод показаний
│   │
│   └── accruals/             # Компоненты для начислений
│       └── AccrualsCalculationModal.tsx # Расчет и начисление услуг
│
├── config/                   # Конфигурационные файлы
│   ├── columns.ts           # Конфигурация колонок таблиц
│   ├── formatters.ts        # Функции форматирования данных
│   ├── menu.ts              # Конфигурация меню и ресурсов
│   └── colors.ts            # Цветовая схема приложения
│
├── pages/                    # Страницы приложения
│   ├── GenericList.tsx      # Универсальная страница списка
│   ├── Users.tsx            # Управление пользователями (admin)
│   └── ResidentCabinet.tsx  # Личный кабинет жителя (ЛК: сводка + свои квитанции)
│
├── types/                    # TypeScript типы
│   └── index.ts             # Глобальные типы данных
│
└── hooks/                    # Кастомные хуки
    └── useSidebarState.ts   # Управление состоянием сайдбара
```

---

## 🔐 Переменные окружения

Канонический перечень переменных — файл **`.env.example`** в корне репозитория
(настройки и секреты не хранятся в git, см. `DEPLOY.md §1`).

- Backend-переменные (`DATABASE_URL` либо набор `POSTGRES_*`, `AUTH_SECRET_KEY`,
  `CORS_ORIGINS`, …) читаются из корневого `.env`;
- Frontend-переменные для браузера именуются с префиксом `VITE_` и доступны через
  `import.meta.env.VITE_*` (локально берутся из `frontend/.env`; при сборке подмены
  не нужны — `VITE_API_URL` выставляется в рантайме через nginx-прокси).

Примеры и назначение каждой переменной ищите в `.env.example` и `docker-compose.yml`.

---

## 📄 Описание ключевых файлов

### 1. `App.tsx` - Главный компонент (~50 строк)

**Что делает:**
- Настраивает провайдеров (ConfigProvider, Refine, Router)
- Определяет маршруты приложения
- Отрисовывает общий макет (Sidebar + Content)

**Ключевые особенности:**
```typescript
// Минималистичный, только роутинг и провайдеры
<Refine dataProvider={dataProvider("/api")}>
    <Routes>
        <Route path="/:resource" element={<GenericList />} />
    </Routes>
</Refine>
```

---

### 2. `pages/GenericList.tsx` - Универсальная страница (~1050 строк)

**Что делает:**
- Отображает таблицу с данными для любого ресурса, включая `meter_reading_documents` и `meter_readings`
- Обрабатывает CRUD операции (Create, Read, Update, Delete)
- Управляет модальными окнами
- Интегрируется с метаданными бэкенда

**Ключевые особенности:**
- Динамически строит таблицу на основе конфигурации
- Для ресурса `meter_reading_documents` показывает кнопку «Массовый ввод показаний» и открывает `BulkReadingsModal`
- Для ресурса `accrual_documents` показывает кнопку расчёта начислений и открывает `AccrualsCalculationModal`
- Удаление документов (`meter_reading_documents`, `accrual_documents`) сопровождается предупреждением о каскадном удалении связанных записей регистра
- Регистры (`accounts_register`, `accruals_register`, `meter_readings`) — только чтение и **без столбца «Действия»**: записи формируются документами, прямое редактирование/удаление недоступно
- **Кнопка «Фильтры» (Б10)**: по каждой видимой сортируемой колонке — контроль под тип поля (текст «содержит», «от/до» для чисел, период для дат, Да/Нет для bool, выбор для enum); применяется на сервере через refine `setFilters` (параметры `<field>[_{like|ne|gte|lte}]=<value>`), счётчик активных фильтров на кнопке и «Сбросить». Типы контролов — `config/filters.ts`; состояние списка изолировано по ресурсу (`key` в App.tsx + сброс при смене `resourceName`). Доработки: колонки-справочники (касса/счёт, статья, контрагент, собственник, л/с, квартира, вид услуги, счётчик, тип тарифа) фильтруются выпадающим списком со значениями из того же справочника (`components/common/ReferenceFilterSelect.tsx`, кэш по ресурсу); ресурсо-зависимые select-поля («Статус» в «Тарифах» — Действующий/Архивный); стартовые фильтры раздела (default-фильтры из `config/filters.ts` — «Тарифы» по умолчанию показывают «Действующие»).
- **Единая компоновка (2.12) + кнопки-иконки (Б7)**: колонка «Действия» из строк убрана; строка выделяется (radio), под заголовком слева — панель действий записи (`renderRecordActions`: просмотр/редактирование/PDF/удаление с подтверждением/отмена списания — по типу раздела и правам роли), справа над таблицей — тулбар: селекты сортировки (колонка + направление), «Фильтры»/«Колонки» (иконки с tooltip, счётчик фильтров — Badge), «Добавить» и спец-действия (начислить/сформировать квитанции/списание/массовые показания). В регистрах выбора нет.
- **Кнопка «Колонки» + drag&drop (2.10-C)**: чек-боксы видимости и перестановка порядка — перетаскиванием строк в панели «Отображаемые колонки» и перетаскиванием самих заголовков таблицы (`components/common/SortableColumns.tsx`: `SortableColumns` + `ColumnHeader`/`ColumnDragTitle`, `@dnd-kit/*`). Настройки колонок (видимость/порядок/ширины) хранятся **на сервере per-user + локальном кэше** (2.13, `auth/preferences.ts`; старый localStorage-формат переносится при первом сохранении); `hooks/useColumnSettings.ts` — чистая логика над настройками, приходящими извне. «ID»/«Действия» всегда видны и не перемещаются; короткий клик по заголовку = сортировка, сдвиг от 5px = перетаскивание.
- **Ресайз ширины колонок (2.1)**: ручка на правом краю заголовка (`ColumnHeader`) тянет ширину; реализация на pointer-событиях (ширина = стартовая ширина `th` + дельта указателя) — работает при `scroll={{x:'max-content'}}`, библиотека не нужна. Ширины сохраняются вместе с порядком/видимостью.
- **Серверные настройки списка (2.13)**: при работе со списком сохраняются и восстанавливаются сортировка, применённые фильтры (с восстановлением панели «Фильтры»), колонки и pageSize — у каждого пользователя свои, переживают перезагрузку и смену устройства.
- «Регистр начислений» по умолчанию отсортирован по «Периоду» (свежие месяцы сверху) — иначе первыми шли «входящие остатки» 2017 г. (Фонд развития, без показаний).

---

### 3. `config/columns.ts` - Конфигурация колонок

**Что делает:**
- Определяет колонки для каждого ресурса
- Применяет форматтеры к данным
- Предоставляет утилиты для работы с колонками

**Пример конфигурации:**
```typescript
export const columnsConfig: Record<string, Column[]> = {
    apartments: [
        { key: 'apartment_number', label: '№ квартиры' },
        { key: 'square', label: 'Площадь, м²', format: formatNumber },
    ],
    meter_reading_documents: [
        { key: 'title', label: 'Название' },
        { key: 'reading_date', label: 'Дата показаний', format: formatDate },
        { key: 'services_type.services_type', label: 'Вид услуги' },
        { key: 'readings_count', label: 'Количество записей' },
    ],
    meter_readings: [
        { key: 'apartment.apartment_number', label: '№ квартиры' },
        { key: 'reading', label: 'Показание', format: formatNumber },
        { key: 'document.title', label: 'Документ' },
    ],
    // ...
};
```

---

### 4. `config/formatters.ts` - Форматтеры данных

**Что делает:**
- Форматирует даты, числа, булевы значения
- Единое место для всех форматов
- Используется во всем приложении

**Доступные форматтеры:**
- `formatDate` - ДД.ММ.ГГГГ
- `formatDateTime` - ДД.ММ.ГГГГ ЧЧ:ММ:СС
- `formatNumber` - 1 234 567
- `formatBool` - Да/Нет
- `formatPrice` - 1 234 567 ₸

---

### 5. `components/common/RecordFormModal.tsx` - Модалка CRUD

**Что делает:**
- Динамически строит форму на основе метаданных
- Поддерживает все типы полей (текст, число, дата, enum, reference)
- Обрабатывает отправку данных

**Поддерживаемые типы:**
- `text` - текстовое поле
- `integer` - число без дробной части
- `decimal` - число с дробной частью
- `date` - выбор даты
- `boolean` - переключатель
- `enum` - выпадающий список
- `reference` - выбор из справочника

---

### 6. `components/common/ReferenceSelect.tsx` - Выбор справочников

**Что делает:**
- Загружает данные из справочника (напрямую через `authedFetch` с `?_end=100000`, в обход Refine-пагинации — поэтому в списке **все** записи, а не первые 10)
- Форматирует отображение записей
- Поддерживает поиск (по отформатированной подписи)
- Значение всегда управляемое (строковый sentinel `""` — пусто), `allowClear` и пункт «— пусто —» надёжно очищают поле
- Для **необязательных** полей (`optional=true`, напр. «Квартира» в Приход/Расход) в начало списка добавляется пункт **«— пусто —»** — его выбор очищает значение (операция «без квартиры»), а не только крестик
- `filterFn` — дополнительная фильтрация записей (напр. статьи аналитики только по типу операции: приход → «Доход», расход → «Расход»)

**Примеры форматтеров:**
```typescript
owners: (item) => item.full_name
apartments: (item) => `№ ${item.apartment_number} — ${item.owner?.full_name}`
accounts: (item) => `${item.account_number} (${item.account_name})`
```

---

### 7. `components/meter-readings/BulkReadingsModal.tsx`

**Что делает:**
- Массовый ввод показаний счетчиков с автоматическим созданием документа-шапки (`meter_reading_documents`)
- Показывает список всех квартир
- Позволяет вводить показания для каждой квартиры
- Автоматически проверяет ошибки
- На бэкенде автоматически подбирается счётчик по квартире и виду услуги, если он явно не указан

**Бизнес-логика:**
1. Оператор вводит название документа (например, «Показания за август 2026»)
2. Выбирает вид услуги (вода, свет, газ) и дату показания
3. Вводит показания напротив каждой квартиры
4. Пустые строки пропускаются
5. POST `/api/meter_readings/bulk` создаёт один `MeterReadingDocument` и набор связанных с ним `MeterReading`
6. Ошибки по отдельным строкам показываются в таблице, не прерывая сохранение остальных строк

Модалка работает в двух режимах:
- **Создание** (без `documentId`): оператор заполняет форму, создаётся новый документ показаний.
- **Редактирование** (с `documentId`): данные и показания существующего документа подгружаются через `GET /api/meter_reading_documents/{id}/readings`, а при сохранении документ полностью пересоздаётся через `PUT /api/meter_reading_documents/{id}/full` (старые строки удаляются, создаются новые из присланного списка).

---

### 8. `components/accruals/AccrualsCalculationModal.tsx`

**Что делает:**
- Расчет начислений за период
- Предварительный просмотр
- Выборочное сохранение

**Бизнес-логика:**
1. Оператор выбирает месяц/год
2. Система рассчитывает начисления по всем счетам (GET `/api/accruals_register/calculate`)
3. Отображается таблица с предварительными строками
4. Оператор выбирает строки для сохранения
5. Выбранные строки сохраняются в регистр (POST `/api/accruals_register/generate`)

Модалка работает в двух режимах:
- **Создание** (без `documentId`): создаётся новый документ начислений.
- **Редактирование** (с `documentId`): месяц/год и ранее выбранные пары (account_id + services_type_id) подгружаются через `GET /api/accrual_documents/{id}/details`, а при сохранении строки документа полностью пересоздаются через `PUT /api/accrual_documents/{id}/full` (старые строки и связанные записи `accounts_register` удаляются).

**Важно (защита от подмены данных):** клиент отправляет на сервер только идентификаторы выбранных пар `(account_id, services_type_id)`. Сумма, потребление, тариф и показания всегда пересчитываются на сервере на момент сохранения, поэтому изменившиеся между предпросмотром и сохранением данные не искажаются.

---

### 9. `types/index.ts` - Глобальные типы

**Основные типы:**
```typescript
// Колонка таблицы
type Column = { key: string; label: string; format?: Function }

// Метаданные поля
type FieldMeta = { name: string; label: string; type: string; required: boolean }

// Состояние модалки
type ModalState = { mode: 'create' | 'edit'; record?: any }

// Строка начислений
type AccrualPreviewRow = { row_number: number; amount: number; /* ... */ }
```

---

## 🔄 Поток данных

### 1. Загрузка списка
```
Пользователь открывает страницу
    ↓
GenericList → useTable() → API запрос
    ↓
Получение данных + метаданных
    ↓
Отображение таблицы
```

### 2. Создание записи
```
Пользователь нажимает "Добавить"
    ↓
RecordFormModal → поля из метаданных
    ↓
Пользователь заполняет форму
    ↓
useCreate() → POST /api/{resource}
    ↓
Обновление таблицы
```

### 3. Массовый ввод показаний
```
Пользователь открывает модалку из раздела «Документы показаний»
    ↓
BulkReadingsModal → загрузка квартир и видов услуг
    ↓
Пользователь вводит название документа, вид услуги, дату и показания
    ↓
POST /api/meter_readings/bulk
    ↓
Создаётся MeterReadingDocument (шапка) + набор MeterReading (строки регистра),
счётчик для каждой квартиры подбирается автоматически, если не указан явно
    ↓
Обработка ошибок поотдельным строкам и успех
```

Удаление документа показаний (`DELETE /api/meter_reading_documents/{id}`) каскадно удаляет все связанные с ним записи в `meter_readings` на уровне БД (`ondelete="CASCADE"`).

### 4. Расчет начислений
```
Пользователь выбирает период
    ↓
GET /api/accruals_register/calculate
    ↓
Отображение предварительных строк
    ↓
Пользователь выбирает строки
    ↓
POST /api/accruals_register/generate
    ↓
Сохранение в регистр
```

---

## 🎨 Особенности UI

### Типы полей в формах:

| Тип | Компонент | Пример |
|-----|-----------|--------|
| `text` | TextArea | Примечание |
| `string` | Input | Название |
| `integer` | InputNumber | Количество |
| `decimal` | InputNumber (step 0.01) | Сумма |
| `date` | DatePicker | Дата начисления |
| `boolean` | Switch | Активен |
| `enum` | Select | Тип операции |
| `reference` | ReferenceSelect | Лицевой счет |

### Форматирование в таблицах:

| Тип | Формат | Пример |
|-----|--------|--------|
| Дата | ДД.ММ.ГГГГ | 24.08.2026 |
| Дата+время | ДД.ММ.ГГГГ ЧЧ:ММ:СС | 24.08.2026 14:30:00 |
| Число | 1 234 567 | 1 234 567.50 |
| Булево | Да/Нет | Да |
| Цена | 1 234 567 ₸ | 1 234 567 ₸ |

---

## 🗄️ Бэкенд (`backend/`)

### Стек: FastAPI + SQLAlchemy + PostgreSQL

### `models.py` — модели СУБД
Ключевые модели, реализующие разделение документ/регистр:

| Модель | Таблица | Роль |
|-------|--------|------|
| `MeterReadingDocument` | `meter_reading_documents` | Документ-шапка: название, дата, вид услуги |
| `MeterReading` | `meter_readings` | Строка регистра: конкретное показание, ссылается на `document_id` |
| `AccrualDocument` | `accrual_documents` | Документ-шапка начислений: дата, `title` (название) |
| `AccrualsRegister` | `accruals_register` | Строка регистра начислений, ссылается на `accrual_document_id` |

`MeterReading` также хранит `apartment_id` напрямую (не только через `meter_id`), что позволяет вносить показания даже без привязанного счётчика.

Удаление документа каскадно удаляет связанные строки регистра (`ForeignKey(..., ondelete="CASCADE")`).

### `app.py` — API
- Универсальные CRUD-эндпоинты `GET/POST/PATCH/DELETE /api/{resource}` через `MODEL_MAP` / `SERIALIZERS` / `FIELD_CONFIG`
- `GET /api/meta/{resource}` — метаданные полей для динамической формы на фронте
- Общий серверный фильтр списков (Б10): `backend/filtering.py` — GET принимает `<field>[_{like|ne|gte|lte}]=<value>` (формат simple-rest); разрешение полей как у сортировки (прямые столбцы, вложенные по relationship, агрегаты, автор), автотипизация bool/числа/даты/datetime, contains — ILIKE (для нестроковых — CAST AS text), нативные PG-enum — eq/ne по имени члена; неразрешимые параметры молча игнорируются
- Кастомные билдеры значений (`CUSTOM_VALUE_BUILDERS`): для `transactions`/`payments` и `meter_readings` сервер сам подбирает лицевой счёт/счётчик по квартире и виду услуги
- Ресурс `meters` (2.7): при создании/изменении generic CRUD валидирует, что у выбранного «Вида услуги» тип тарифа «По счетчику» (`validate_meter_service_type` из `services.py` → 422); сериализатор `services_type` отдаёт признак `has_meter_tariff`, по нему фронт фильтрует «Вид услуги» в форме «Счетчики»
- Документы «Приход/Расход» (`transactions`/`payments`) имеют поле `title` — название формируется автоматически по формуле «Тип операции + №(ID) + дата операции» (например, «Приход в кассу №17 от 24.08.2026») в момент создания и пересчитывается при изменении типа/даты операции
- `POST /api/meter_readings/bulk` — создаёт `MeterReadingDocument` и набор `MeterReading` за один запрос, автоподбор счётчика по квартире/виду услуги
- `GET /api/meter_reading_documents/{id}/readings` — детализация показаний конкретного документа
- `PUT /api/meter_reading_documents/{id}/full` — полное пересоздание документа показаний (редактирование)
- `GET/POST/PATCH/DELETE /api/accrual_documents` — жизненный цикл документов начислений
- `GET /api/accrual_documents/{id}/details` и `PUT /api/accrual_documents/{id}/full` — детализация и полное пересоздание строк документа начислений
- `GET /api/accruals_register/calculate` и `POST /api/accruals_register/generate` — расчёт и сохранение начислений в регистр
- Ресурс `tariffs`: тариф имеет статус «Действующий/Архивный» (`tariffs.status`, миграция `0014_tariff_status`; сериализатор отдаёт `status`). При создании нового тарифа предыдущие «Действующие» той же группы (вид услуги + признак разовости) автоматически помечаются «Архивными» (`services.retire_tariff_predecessors` вызывается из generic create/update); статус информационный — расчёт начислений от него не зависит

Документы начислений имеют поле `title` (название): оно сериализуется, принимается при создании/обновлении/генерации, а при отсутствии авто-генерируется из `accrual_date` (например, «Начисление за август 2026»).

Регистр `accounts_register` закрыт для прямого создания/изменения/удаления (403) — записи формируются исключительно документами (платежи через SQLAlchemy `event` на `Transaction`, начисления через документы начислений). Строки начислений дополнительно хранят `services_type_id` (вид услуги), который заполняется при записи документа начисления в регистр и отображается в списке/формах. Балансы (`balance_after`) пересчитываются «с нуля» по истории операций аккаунта: при любой вставке/изменении/удалении платежа или пересоздании/удалении начислений выполняется кумулятивный пересчёт всех записей аккаунта в хронологическом порядке (`operation_date`, затем `id`), а не «последняя запись + дельта». Это гарантирует согласованность сальдо даже после правок/удалений операций.

Регистры показывают название документа-источника (`document_title`): в `accruals_register` — название документа начислений (`accrual_document.title`); в `accounts_register` — название начисления либо название документа «Приход/Расход» (`transaction.title`), в зависимости от того, что создало запись.

**Квитанции** (`receipt_documents` + `receipt_items`): формируются массово по всем активным лицевым счетам за период из строк начислений. Документ-шапка хранит реквизиты плательщика и итоги (начислено / долг / переплата / к оплате), строки — услуги с показаниями, тарифом и суммой. Текущий баланс счёта (долг или переплата) «садится» на строку «Фонд развития» (услуга определяется по имени; если её нет в начислениях — добавляется отдельно). PDF генерируется на лету через `reportlab` (эндпоинт `GET /api/receipt_documents/{id}/pdf`, вёрстка в стиле образца «Квитанция.pdf» со столбцом «Переплата»).

Протокол списков совместим с `@refinedev/simple-rest`: параметры `_start/_end/_sort/_order` и фильтры `<field>[_{like|ne|gte|lte}]=<value>` (см. `backend/filtering.py`); ответ — JSON-массив с заголовком `X-Total-Count`.

**Личный кабинет жителя (ЛК):**
- `users.account_id` — привязка пользователя к лицевому счёту (миграция `0003_resident_account_link`, поле добавлено в API пользователей и `create_user.py --account`).
- `GET /api/me/statement` — сводка по собственному счёту (для `resident` счёт берётся из `account_id`, id знать не нужно).
- `GET /api/me/receipts` — список своих квитанций.
- Доступ ограничен: `resident` видит только свой счёт и свои квитанции (403 на чужие) — в `routers/others.py` (`_ensure_can_view_account`) и `routers/receipts.py` (`_raise_for_resident_other` для `statement`/`items`/`pdf`). Админ/оператор/кассир не ограничены.
- Фронт: страница `ResidentCabinet.tsx` (сводка + детализация по услугам + список квитанций с просмотром и PDF), раздел меню «Мой кабинет» — только роль `resident` (`menuAccess.ts` → `cabinet`).
- Seed (`seed_17_apartments.py`) привязывает пользователя `resident` к `FTH-001` для ЛК.

### Роли и права доступа

Роли системы: `admin` / `operator` / `cashier` / `controller` / `resident` / `auditor` (ролевая матрица — `ROADMAP.md`).

- **`auditor` («Аудитор», 09.2026)** — read-only: видит все разделы, регистры и отчёты, включая ЛК/выписку любого лицевого счёта (`GET /api/accounts/{id}/statement`, `cabinet_admin`).
  Запись запрещена на всех эндпоинтах: generic CRUD — `permissions.py` (`_read_allowed` для чтения, запись/удаление — `False`); кастомные write-эндпоинты документов/квитанций — зависимость `require_write_access` (`routers/documents.py`, `routers/receipts.py`); настройки и генерация/пересбор регистров недоступны. Отчёты и чтение регистров разрешены (`routers/reports.py`).
- Фронт: меню аудитора показывает все разделы, кроме «Пользователи и права» и ЛК жителя (`menuAccess.ts`); кнопки «Добавить/Изменить/Удалить» скрыты (`can.ts`).

### Запуск через nginx
```
Браузер → nginx (ваш домен)
    → location /        → 127.0.0.1:5173 (Vite dev-server)
    → location /api/    → 127.0.0.1:8000 (FastAPI, без отрезания префикса!)
```
Важно: `proxy_pass` для `/api/` должен быть без завершающего слэша (`http://127.0.0.1:8000`, а не `http://127.0.0.1:8000/`), иначе nginx срезает префикс `/api/`, а бэкенд (`APIRouter(prefix="/api")`) его ожидает.

---

## 🚀 Команды для разработки

Быстрый старт бэкенда + БД (+ Alembic/справочники/админ) — одной командой:

```bash
./scripts/dev.sh          # развернуть и запустить API на http://localhost:8000
./scripts/dev.sh --full   # дополнительно: pip install -r backend/requirements.txt
```

Фронтенд (dev-сервер Vite, http://localhost:5173):

```bash
cd frontend
npm install
npm run dev          # dev-сервер Vite (HMR)
npm run build        # сборка для продакшена (tsc + vite build)
npx tsc --noEmit     # проверка TypeScript (эквивалент tsc-шага сборки)
```

Запуск всего стека в Docker (`postgres`+`backend`+`frontend`+`nginx`) и типовые
команды (`docker compose restart/logs/up -d --build …`) — в `DEPLOY.md §7`;
развёртывание на сервере — `DEPLOY.md §3` и `scripts/` (`setup_vps.sh`, `deploy_vps.sh`).

Примечание: скрипта `npm run lint` нет — ESLint не подключён как зависимость (в проекте есть только idle-конфиг `eslint.config.js`).

---

## 📝 Соглашения по коду

### Именование файлов:
- Компоненты: `PascalCase.tsx` (RecordFormModal.tsx)
- Утилиты: `camelCase.ts` (formatters.ts)
- Конфигурация: `camelCase.ts` (columns.ts)
- Хуки: `useCamelCase.ts` (useSidebarState.ts)
- Типы: `index.ts` (в папке types)

### Именование компонентов:
- Компоненты: `PascalCase`
- Хуки: `useCamelCase`
- Пропсы: `PascalCaseProps` (RecordFormModalProps)
- Обработчики: `handleCamelCase` (handleSubmit)

### Структура компонента:
```typescript
// 1. Импорты (сначала сторонние, потом локальные)
import { useState } from "react";
import { Button } from "antd";
import { useApiUrl } from "@refinedev/core";
import { MyComponent } from "./MyComponent";

// 2. Интерфейсы/Типы
interface MyComponentProps {
    prop1: string;
    prop2?: number;
}

// 3. Компонент
export const MyComponent = ({ prop1, prop2 }: MyComponentProps) => {
    // Состояния
    const [state, setState] = useState();
    
    // Эффекты
    useEffect(() => {
        // ...
    }, []);
    
    // Обработчики
    const handleClick = () => {
        // ...
    };
    
    // Рендер
    return (
        <div>
            {/* JSX */}
        </div>
    );
};

// 4. Экспорт по умолчанию (если нужно)
export default MyComponent;
```

---

## 🔧 Расширение функциональности

### Добавление нового ресурса:
1. Добавить модель в `backend/models.py` и включить в `MODEL_MAP`/`SERIALIZERS`/`FIELD_CONFIG` в `backend/app.py`
2. Добавить в `config/menu.ts`
3. Добавить колонки в `config/columns.ts`
4. Добавить форматтер в `config/formatters.ts` (если нужно)
5. Готово! GenericList автоматически поддержит новый ресурс

### Добавление новой пары «документ + регистр» (по аналогии с показаниями/начислениями):
1. Создать две модели: `XxxDocument` (шапка) и `Xxx` (строка регистра с `document_id`, `ondelete="CASCADE"`)
2. Добавить оба ресурса в `MODEL_MAP`/`SERIALIZERS`/`FIELD_CONFIG`
3. При необходимости добавить отдельный bulk-эндпоинт для массового создания строк вместе с документом (по аналогии с `/api/meter_readings/bulk`)
4. В `GenericList.tsx` добавить условие для нового ресурса-документа (кнопка массового действия, предупреждение о каскадном удалении)

### Добавление нового типа поля:
1. Добавить тип в `types/index.ts` (FieldMeta.type)
2. Добавить обработку в `renderFieldControl.tsx`
3. Добавить обработку в `RecordFormModal.tsx`

### Добавление нового компонента:
1. Создать файл в соответствующей папке
2. Импортировать в нужный компонент
3. Добавить экспорт в index.ts папки (если есть)

---

## 🐛 Частые проблемы и решения

### Проблема: HMR не обновляется
```bash
# Очистка кеша Vite
rm -rf /opt/townhouse/frontend/node_modules/.vite
docker compose restart frontend
```

### Проблема: Ошибка импорта
```bash
# Проверка структуры файлов
find /opt/townhouse/frontend/src -name "*.tsx" -o -name "*.ts"

# Проверка путей в импортах
grep -r "from \"\./" /opt/townhouse/frontend/src/
```

### Проблема: Нет данных в таблице
```bash
# Проверка API
curl http://localhost:5173/api/{resource}

# Проверка логов
docker compose logs frontend --tail=50
docker compose logs backend --tail=50
```

### Проблема: TypeScript ошибки
```bash
# Проверка всех ошибок
npx tsc --noEmit

# Проверка конкретного файла
npx tsc --noEmit src/App.tsx
```

### Проблема: Конфликт зависимостей
```bash
# Удаление node_modules и переустановка
rm -rf node_modules package-lock.json
npm install
```

---

## 📚 Полезные ссылки

### Документация:
- [Refine Documentation](https://refine.dev/docs/)
- [Ant Design Components](https://ant.design/components/overview/)
- [React Router](https://reactrouter.com/en/main)
- [Vite](https://vitejs.dev/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)

### Инструменты:
- [React DevTools](https://react.dev/learn/react-developer-tools)
- [Vite Plugin Inspector](https://github.com/antfu/vite-plugin-inspect)

---

## 📊 Статистика проекта

### Файловая структура:
- **Всего файлов:** 21 (фронтенд `src/`) + бэкенд `backend/`
- **Компонентов:** 11
- **Конфигураций:** 4
- **Типов:** 1
- **Хуков:** 1

### Размеры ключевых файлов:
| Файл | Строк | Назначение |
|------|-------|------------|
| App.tsx | ~70 | Главный компонент |
| GenericList.tsx | ~520 | Универсальная страница |
| RecordFormModal.tsx | ~140 | Модалка CRUD |
| BulkReadingsModal.tsx | ~310 | Массовый ввод показаний + редактирование документа |
| AccrualsCalculationModal.tsx | ~350 | Расчёт начислений + редактирование документа |
| backend/models.py | ~380 | Модели СУБД (включая `MeterReadingDocument`) |
| backend/app.py | ~1720 | API: CRUD + bulk-эндпоинты для показаний/начислений |

---

*Последнее обновление: Сентябрь 2026*

---

## 📝 Список изменений

| Дата | Изменение |
|------|-----------|
| Август 2026 | Создан документ |
| Август 2026 | Добавлен раздел "Переменные окружения" |
| Август 2026 | Добавлен раздел "Статистика проекта" |
| Август 2026 | Добавлены новые команды для разработки |
| Август 2026 | Обновлены соглашения по коду |
| Август 2026 | Добавлен formatPrice в форматтеры |
| Август 2026 | Разделение показаний и начислений на документы и регистры: добавлена модель/таблица `MeterReadingDocument`, `MeterReading` связан с документом через `document_id`, добавлен эндпоинт `POST /api/meter_readings/bulk` для массового ввода, обновлён GenericList/BulkReadingsModal/columns.ts/menu.ts, исправлена конфигурация nginx (префикс `/api/`) |
| Август 2026 | Добавлено редактирование документов: `PUT /api/meter_reading_documents/{id}/full` и `PUT /api/accrual_documents/{id}/full` (полное пересоздание строк), `GET /api/accrual_documents/{id}/details`; серверный пересчёт сумм/потребления/тарифа при сохранении начислений; регистр `accounts_register` закрыт для прямой записи (403) |
| Август 2026 | Доделан `title` у `AccrualDocument`: сериализуется, принимается при создании/PATCH/генерации/редактировании, авто-генерируется из `accrual_date` при отсутствии; добавлено поле «Название документа» в `AccrualsCalculationModal` и колонка в списке. Удалены артефакты `backend/test_conn.py` и `backend/templates/` |
| Август 2026 | Целостность `accounts_register`: балансы пересчитываются «с нуля» по истории операций аккаунта кумулятивно (оконная функция через `recalculate_account_balance`) при вставке/изменении/удалении платежа и пересоздании/удалении начислений; исправлен «дрифт» последующих сальдо при правке/удалении операций |
| Август 2026 | Поле «Документ» во «Взаиморасчётах» привязывает строки списания к документу «Приход/Расход» (`transaction_id`) для отображения названия прихода/расхода |
| Август 2026 | Личный кабинет жителя (ЛК): `users.account_id` (миграция `0003_resident_account_link`), `GET /api/me/statement` и `GET /api/me/receipts`, доступ `resident` только к своему счёту/квитанциям; фронт `ResidentCabinet.tsx` (сводка + свои квитанции с просмотром/PDF), меню «Мой кабинет» только для `resident`; seed привязывает `resident` к `FTH-001` |
| Август 2026 | В `accounts_register` добавлен «Вид услуги» (`services_type_id`): колонка в БД, заполняется при записи документа начисления в регистр (и при перезаписи/редактировании), сериализуется и отображается в списке/формах |
| Август 2026 | Документ «Приход/Расход» получил поле «Название» (`title`): формируется автоматически по формуле «Тип операции + №(ID) + дата операции» при создании и пересчитывается при изменении типа/даты; добавлено в списки и формы (поле только для чтения) |
| Август 2026 | В форму «Приход/Расход» добавлено редактируемое поле «Дата» (`transaction_date`): заполняется при создании (по умолчанию сегодня), меняется при редактировании; при изменении даты название документа и дата операции в регистре взаиморасчётов пересчитываются |
| Август 2026 | Из форм регистров (`accounts_register`, `accruals_register`, `meter_readings`) убран столбец «Действия» — регистры только для чтения, без прямого редактирования/удаления (добавлен общий флаг `isRegister` в GenericList) |
| Август 2026 | В регистры добавлен «№ квартиры»: в `accruals_register` и `accounts_register` добавлена колонка и сериализация `apartment.apartment_number`; исправлена серверная сортировка по «№ квартиры» во всех трёх регистрах (`accruals_register`, `accounts_register`, `meter_readings`) через подзапросы |
| Август 2026 | Приведён к единому виду порядок колонок в похожих формах: регистры — «№ квартиры → лицевой счёт → вид услуги → дата → показатели → документ»; документы-шапки — «название → дата → специфика → дата создания» (в конце) |
| Август 2026 | Настройка колонок в формах: ручное изменение ширины колонок (за правый край заголовка) и перестановка порядка (drag-and-drop за ручку «≡»); порядок и ширины сохраняются в localStorage отдельно на каждый ресурс |
| Август 2026 | Перестановка колонок (drag-and-drop) отключена из-за неработоспособности; оставлен только ресайз ширины колонок (сохраняется в localStorage на ресурс). Код drag-and-drop удалён |
| Август 2026 | Нерабочий ресайз ширины колонок также удалён: таблица возвращена к исходному виду (без ручных хендлеров заголовков). Файлы ColumnHeader.tsx и useColumnConfig.ts удалены |
| Август 2026 | Квитанции: модели `receipt_documents`/`receipt_items` (документ + строки), генерация по всем счетам за период, долг/переплата «садится» на «Фонд развития» (ID 7), PDF на лету через reportlab (столбец «Переплата»), фронт: меню, ReceiptsModal, кнопка PDF |
| Август 2026 | Квитанции (доработка PDF): логотип Family Townhouse в шапке, двухуровневая шапка таблицы с объединённой ячейкой «Показания» (над столбцами «Посл.»/«Пред.»), таблица помещается на лист A4 |
| Август 2026 | Квитанции: массовое скачивание за период одним ZIP (`POST /receipt_documents/bulk_pdf`) и массовое удаление за период (`DELETE /receipt_documents/bulk_delete`); кнопки в ReceiptsModal |
| Август 2026 | В регистры добавлен столбец «Документ» (`document_title`): `accruals_register` — название документа начислений, `accounts_register` — название начисления или «Приход/Расход»; заголовок «Документ» — единообразно с «Регистр показаний»; добавлена серверная сортировка по этому столбцу |
| Август 2026 | Отчёт по кассе (2.11): пункт меню «Отчеты → По кассе», бэкенд `GET /api/reports/cash_register` (остаток на начало / приход / расход / остаток на конец по кассам + движение по документам с аналитикой), фронт `CashReport.tsx` |
| Август 2026 | Справочник «Аналитика» (`analytic_articles`): единые статьи доходов/расходов, `transactions.article_id`; поле «Квартира» в «Приход/Расход» стало необязательным (`account_id=NULL` — операция без квартиры идёт в общий `cash_register`); статья аналитики обязательна, фильтр статей по типу операции (приход→«Доход», расход→«Расход»), автосброс статьи при смене типа, поле за Типом операции; (поле `code` из аналитики убрано) |
| Август 2026 | Настройка видимых колонок в списках (2.10, Вариант A): кнопка «Колонки» (чек-боксы по `columnsConfig`), сохранение в localStorage по (ресурс, роль), колонки «ID»/«Действия» всегда видны |
| Август 2026 | UI справочников (`ReferenceSelect`): показываются все записи вместо первых 10 (обход Refine-пагинации через `authedFetch`), значение всегда управляемое, для необязательных полей (квартира) добавлен пункт «— пусто —» — очистка значения выбором из списка, а не только крестиком |
| Август 2026 | Общее решение серверной сортировки по вложенным полям (1.5): `backend/sorting.py` — декларативные пути по relationship, авто-генерация коррелированных SQLAlchemy-подзапросов (`.correlate()`) вместо рукописного SQL; поддержка агрегатов, автора и составных полей; убран фоллбек `hasattr` для вложенных полей; исправлен 500 при сортировке квитанций по автору |
| Сентябрь 2026 | **Раздел 4 «Миграция данных из старой БД»** — перенос истории из `templates/Миграция данных FTH.xlsx` на чистую схему. Подготовка/очистка: `backend/migrations/migrate_prepare_sources.py` → `templates/clean/*.csv` (справочники владельцы/квартиры, метры с перенумерацией приборов, журнал показаний, начисления с типами reg/vary/razov/manual/enter и признаками tariff/consumption, касса с чistкой). Синтез: `backend/migrations/migrate_import.py` (справочники+метры) и `backend/migrations/migrate_synthetic/{tariffs,readings,accruals,initial_balance,cash,rebuild}.py` (#,# #), порядок и команды — в `backend/migrations/README.md`. Входящие старт.сальдо вносятся как первичные строки 2017-10; касса через прямой SQL (без ORM-событий); авто-названия и тарифы разовых акций на месте. Проверено контрольными суммами на чистой dev-БД. |
| Сентябрь 2026 | **Отчёт по кассе (починка)** — SQL-запрос ссылался на переименованную таблицу `owners` (теперь `counterparties`), из-за чего отчёт падал во всех Эндпоинтах; поправлен на `LEFT JOIN counterparties`. Вычищены «фантомные» тестовые кассы. |
| Сентябрь 2026 | **Отчёт по расходам (2.11б)** — реализован: `GET /api/reports/expenses` (расход кассы за период: итог, по статьям расходов, детализация по документам/контрагентам), фронт `ExpenseReport.tsx`, пункт меню «Отчеты → По расходам» для ролей admin/operator/cashier. |
| Сентябрь 2026 | **Отчёты 2.11 (в), (г)** — «Выписка по счёту» (`GET /api/reports/statement`, фронт `StatementReport.tsx`: помесячно начислено/списано/остаток) и «По должникам» (`GET /api/reports/debtors`, фронт `DebtorsReport.tsx`: активные л/с с долгом, по убыванию); пункты меню «Отчеты → Выписка по счёту» и «Отчеты → По должникам» для ролей admin/operator/cashier. |
| Сентябрь 2026 | **Приход жителей: свёртка в один документ на счёт/день** — инструмент `migrations/reimport/group_resident_income_by_day.py` (или SQL-прикат 4): повторяющиеся «Поступления от жителей» одного (счёт, касса, день, комментарий) сливаются в один документ, лишние удаляются, регистр взаиморасчётов пересобирается. Убрано 7530 построчных записей; transactions/cash сократились с 10140 до 2609. |
| Сентябрь 2026 | **Данные перезалиты в «хроно-порядок» (dev-референс)** — цепочка миграционных прикатов (артефакты `migrations/output/` + скрипты `migrations/reimport/*`): начисления/квитанции пересозданы по хронологии (стартин остаток id=1), квитанции «К оплате» = начислено + долг на начало − переплата (`since=start`), `receipts` по «Дата документа + № квартиры», «Приход/Расход» перенумерован → «Начальный остаток» id/doc_no=1, приходы жителей свёрнуты. ID таблиц сохранены без дырок (последовательности = max+1). |
| Сентябрь 2026 | **Сборка/восстановление БД (+ DEPLOY)** — `scripts/restore_townhouse.sh` (data[--fresh]/roles/all), разделы DEPLOY §8.1/8.2; локальные дампы `backend/backups/townhouse_*.sql`/`townhouse_roles_*.sql` (плюс дамп до чистки) скрыты `.gitignore`. |
| Сентябрь 2026 | **Отчёты по кассе/расходам и серверная сортировка** — уточнения в кодовой базе (см. changelog выше); колонка «Тип/Примечание» «Начисления» стали сортируемыми; добавлен скрипт восстановления и файлы разовых накатов `backend/migrations/output/{apply_*,recreate_*}.sql` (данные не трекаются). |
| Сентябрь 2026 | **Роль «Аудитор» (read-only)** — новая роль `auditor` (models.UserRole; БД-миграция не нужна — `users.role` VARCHAR): просмотр всех разделов/регистров/отчётов и ЛК/выписки любого счёта; запись запрещена (generic CRUD — `permissions._read_allowed`; кастомные write-эндпоинты документов/квитанций — `require_write_access` в `routers/documents.py`/`receipts.py`; спец-операции регистров/настроек недоступны). Фронт: меню аудитора — все разделы кроме «Пользователи и права» и ЛК жителя (`menuAccess.ts`), кнопки изменений скрыты (`can.ts`); роль добавлена в UI пользователей (`Users.tsx`). Создан пользователь `audit`. |
| Сентябрь 2026 | **Доступ из Интернета (домашний ПК)** — в `docker-compose.yml` добавлен сервис `nginx` (80/443): статика production-сборки `frontend/dist` + прокси `/api/` на backend (конфиг `deploy/nginx-home.conf`); `CORS_ORIGINS` пробрасывается в backend из корневого `.env`. Внешний вход: роутер (DMZ off, правила проброса портов) — либо обратный прокси NAS Synology DSM (HTTPS, сертификат DSM) на `http://192.168.50.101:80`. Каталоги сертификатов/ACME (`deploy/certs/`, `deploy/certbot-webroot/`) — в `.gitignore`. |
| Сентябрь 2026 | **Пользователи и ЛК (фиксы)** — заведены сотрудники `operator`/`cashier`/`controller` и жители `fth001…fth017` (привязка к ЛС, `full_name` = ФИО собственника); исправлен вход в ЛК жителя (пересчёт ролей/ресурсов по событию `townhouse:auth-change`), форма входа защищена от двойной отправки; бэкапы БД обновлены (`backend/backups/townhouse_20260904_230601.sql` + roles). |
| Сентябрь 2026 | **CI/CD (п. 3.3 роадмапа)** — добавлены `.github/workflows/ci.yml` (backend: disposable `postgres:16` → `alembic upgrade head` → `init_data.py` → `pytest`; frontend: `npm ci` → `npm run build`), `.github/workflows/docker-build.yml` (публикация образов в `ghcr.io/asagat/townhouse-app-{backend,frontend}`: push в main → тег `main`, тег `v*` → semver), `.github/workflows/deploy.yml` + `.github/actions/deploy` (выкат `scripts/deploy_vps.sh <ref>` по релизному тегу или вручную на staging/production; деплой только при зелёном CI для коммита). `scripts/deploy_vps.sh`: аргумент ветка/тег/SHA + пересборка фронтенда при каждом деплое (раньше статика nginx не обновлялась). Тест сверки с файлом-источником (`test_control_sums_vs_source.py`) пропускается на пустой БД (disposable-БД CI). Документация: DEPLOY.md §10, ROADMAP 3.3/ТД-3. |
| Сентябрь 2026 | **Роадмап: новые заявки владельца** — зафиксированы задачи 2.13–2.17 (отдельное хранение настроек пользователя; разделение «Приходы/Расходы» на ПКО/РКО/платёжное поручение/поступление на счёт; поле «Тип» = Счёт/Касса в «Кассы/Счета»; экспорт всех отчётов в PDF; сортировка столбца только по возр./убыв.); раздел «Что осталось сделать» (ввод CI/CD в строй: секреты/окружения/required checks — DEPLOY.md §10). |
| Сентябрь 2026 | **Б10 — общий механизм фильтрации списков** — бэкенд `backend/filtering.py`: серверная фильтрация generic GET по параметрам `<field>[_{like|ne|gte|lte}]=<value>` (разрешение полей переиспользует `sorting.SORT_FIELDS`: прямые столбцы, вложенные поля, агрегаты, автор; автотипизация bool/числа/даты/datetime, contains — ILIKE, eq/ne нативных PG-enum по имени члена); фронт: кнопка «Фильтры» в `GenericList` (`config/filters.ts`) с контролами по типу колонки (текст/диапазон от-до/период/Да-Нет/выбор), применение через refine `setFilters`, счётчик активных фильтров, «Сбросить»; изоляция состояния списков между разделами (`key` + сброс по resourceName); тесты `backend/tests/test_filtering.py` (46 passed). |
| Сентябрь 2026 | **Б4+Б13 — показания и «Тариф» в «Регистре начислений»** — исторические строки регистра заполнены показаниями (`backend/migrations/fill_accrual_readings.py`, 1745/1748; учёт замены счётчиков через подбор пары с разностью = consumption); при генерации начислений фиксируется `current_reading_id` (`services.py`); в списке регистра добавлена колонка «Тариф» (`tariff.price` + наследуемая `unit`, формат «10 ₸ / м³»). |
| Сентябрь 2026 | **«Тип тарифа» и «Ед. изм.» перенесены с тарифа на вид услуги** — одна привязка на услугу (`services_type.tariff_type_id` NOT NULL FK, `services_type.unit`), наследуется всеми тарифами; колонки `tariffs.tariff_type_id`/`tariffs.unit` удалены (миграции `0012_service_tariff_type`, `0013_service_unit` с бэкафиллом из регулярных тарифов). Мотив: ошибочный тип на часто вводимом тарифе ломал начисление. Расчёт начислений и валидация счётчиков читают тип услуги (`services.calculate_accrual_*`, `service_supports_meter`), `has_meter_tariff` = тип услуги «По счетчику»; сериализаторы услуги/тарифа/`accruals_register` отдают наследуемые тип и ед. изм.; поле «Тип тарифа»/«Ед. изм.» в форме «Виды услуг», из «Тарифов» убраны; сортировка тарифов по этим полям — через вид услуги (`sorting.py`). Контроль: начисление 08.2026 не изменилось (100 строк / 614 680 ₸ до/после), pytest 42 passed. |
| Сентябрь 2026 | **Развёртывание: HTTP-only + зеркальная синхронизация ПК** — внешний nginx домашнего ПК переведён на HTTP (:80), HTTPS-сертификат/ACME удалены (HTTPS — опционально через NAS Synology). DEPLOY §7.2–7.5: схемы ПК «corsus» (docker-стек) и «snowflake» (нативный), симметричный режим «разработка по очереди» с передачей дампов через Synology Drive; скрипты `scripts/dump_to_sync.sh` (выгрузка дампа + маркер), `scripts/refresh_from_backups.sh` (импорт свежих дампов из `DB_MIRROR_BACKUPS`), `scripts/update_db_after_pull.sh` + git-hook `post-merge` (автоимпорт после pull), `scripts/install_pre_push_hook.sh` + hook `pre-push` (автодамп перед push); у `restore_townhouse.sh` флаг `--yes` для неинтерактивной автоматизации. |
| Сентябрь 2026 | **Чистка «Видов услуг» (по решению владельца)** — удалён дубль «Электричество» (`backend/migrations/dedupe_electricity_service.py`, каноническая — «Электроэнергия»); услуга «Прочие расходы» удалена, её 30 исторических начислений (206 520 ₸: парковка/освещение, проверка газа) переведены на «Фонд развития» с разовыми тарифами 6000/7560 ₸ (`move_prochie_rashody_to_fond.py` + `fix_fond_oneoff_tariffs_for_prochie.py`): пересборка `accounts_register`, перегенерация квитанций, контрольные суммы по квартирам/итогам не изменились (60 008 492,00). В `init_data.py` из услуг по умолчанию убраны «Электричество»/«Холодная вода». Итог: в справочнике 6 услуг. |
| Сентябрь 2026 | **Контрольные суммы регистров vs файл-источник** — `backend/tests/test_control_sums_vs_source.py`: начисления (по квартирам/итог/кол-во) и касса (расход; приход по квартире нетто сторно; приход итог + входящее сальдо) сверяются с `templates/Миграция данных FTH.xlsx`; `openpyxl` добавлен в `backend/requirements.txt`; требует файл-источник/пакет (`MIGRATION_SRC_XLSX`), иначе тест пропускается (на пустой БД CI — тоже skip). |
| Сентябрь 2026 | **Б10: фикс «содержит» по вложенным полям + фильтры-справочники** — исправлен 500 на contains/eq по вложенным полям (`backend/filtering.py`: `_kind_of_type` вместо повторного `.type` у path-дескрипторов); колонки-справочники в списках фильтруются select со значениями из того же справочника (`components/common/ReferenceFilterSelect.tsx`, кэш по ресурсу); «Регистр начислений» по умолчанию отсортирован по «Периоду» (свежие сверху). pytest 47 passed. |
| Сентябрь 2026 | **Тарифы: статус «Действующий/Архивный» (Б11)** — колонка `tariffs.status` (миграция `0014_tariff_status`; бэкафилл: действующий — последний тариф группы «вид услуги + разовость»); при создании нового тарифа предыдущие действующие той же группы автоматически архивируются (`services.retire_tariff_predecessors`); UI: колонка «Статус» и фильтр по статусу в «Тарифах», раздел по умолчанию показывает «Действующие» (в `GenericList` добавлены стартовые фильтры раздела и ресурсо-зависимые select-фильтры); тест `tests/test_tariff_status.py`. pytest 48 passed. |
| Сентябрь 2026 | **Порядок колонок списков drag&drop (2.10-C)** — перестановка перетаскиванием самих заголовков таблицы и в панели «Отображаемые колонки»; зависимости `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`; `components/common/SortableColumns.tsx` (`SortableColumns` + `ColumnDragTitle`); `hooks/useVisibleColumns.ts` заменён на `useColumnSettings.ts` (localStorage `{order, hidden}` по (ресурс, роль); старый формат видимости мигрируется; новые колонки не теряются); «ID»/«Действия» закреплены, короткий клик по заголовку — сортировка. |
| Сентябрь 2026 | **«Дата начисления» → «Период» в «Начислениях» и «Регистре начислений»** — поле переименовано и отображается как «Январь, 2025 г.» (`formatPeriod` в `config/formatters.ts`). |
| Сентябрь 2026 | **Ресайз ширины колонок в списках (2.1)** — ручка на правом краю заголовка колонки тянет ширину; реализация на pointer-событиях с pointer capture (ширина = стартовая ширина `th` + дельта движения указателя) — работает при любом `scroll`, включая `max-content`, без npm-библиотеки. Ширины хранятся в localStorage по (ресурс, роль) вместе с порядком/видимостью: `useColumnSettings` теперь хранит `{order, hidden, widths}` (`setWidth`), `GenericList` передаёт `width` в колонки; заголовок собран в `components/common/SortableColumns.tsx` (`ColumnHeader` = drag-заголовок + ресайз-ручка). Ручка не конфликтует с dnd-kit-перетаскиванием заголовков и сортировкой по клику; колонка «Действия» не растягивается. |
| Сентябрь 2026 | **Настройки пользователя (2.13): серверное хранение** — таблица `user_preferences` (user_id, resource, `data` JSON; миграция `0015_user_preferences`), эндпоинты `GET /api/preferences` и `PUT /api/preferences/{resource}` (`backend/routers/preferences.py`). Фронт: `frontend/src/auth/preferences.ts` (fetch/push + локальный кэш по username), `useColumnSettings` переработан в чистую логику (настройки приходят извне), `GenericList` сохраняет/восстанавливает сортировку, применённые фильтры (с восстановлением панели «Фильтры» через `draftFromCrudFilters`), видимость/порядок/ширины колонок и pageSize; изменения на сервер уходят с debounce, при старте/смене устройства сервер — источник правды (старый localStorage-формат видимости переносится при первом сохранении). Тест `backend/tests/test_preferences.py`. |
| Сентябрь 2026 | **Единая компоновка списков (2.12) + кнопки-иконки (Б7)** — колонка «Действия» в строках убрана; строка выделяется (radio/кликом), под заголовком слева — панель действий записи (`renderRecordActions` в `GenericList.tsx`), справа над таблицей — тулбар: селекты сортировки (колонка + направление), «Фильтры»/«Колонки»/«Добавить» и спец-действия в виде иконок с tooltip (счётчик фильтров — Badge); регистры без выбора. Удаление — только с подтверждением. |
| Сентябрь 2026 | **Свёртка приходов в один документ на (л/с, день) и ликвидация отрицательных приходов** — `migrations/reimport/collapse_income_by_day.py`: все приходы одного л/с за день (кроме «Возвратов от контрагентов») сливаются в один документ с суммой-нетто и статьёй «Поступления от жителей»; дни с нетто ≤ 0 удаляются и печатаются списком «требуют разбора»; после — пересчёт `cash_register` и пересборка `accounts_register` (`rebuild_accounts_register`), идемпотентен (dry / `--apply`). Применено: 486 групп, удалено 495 документов-доноров, отрицательных приходов 0 (было 485); итог приходов 58 650 903 ₸ по 17 счетам; квитанции не пересоздавались (нетто не менялось). Далее `renumber_transactions_chronological.py`: id/doc_no перенумерованы в хронологии (2 114 транзакций, «Начальный остаток» = id 1), регистр взаиморасчётов пересобран. Бэкапы до операций — `backend/backups/townhouse_{precollapse,prerenumber}_*.sql`. |
| Сентябрь 2026 | **Формы и списки: денежные/телефонные/датовые форматы, справочные фильтры, кнопки** — единый денежный формат `formatMoney` (разряды + 2 знака) в колонках и формах (ввод через `moneyInputFormatter`/`parser`); телефон с контролем ввода и маской `+7(XXX)XXX-XX-XX` (`normalizePhone`/`formatPhone`, `PhoneInput`); просмотр записей (`RecordFormModal`): readonly-поля без привязки к хранилищу формы + `fieldViewText` (даты `DD.MM.YYYY` без сдвига по ТЗ, булево «Да/Нет», деньги, телефон), фон disabled-полей серый; фильтры-справочники: «Автор» — select пользователей (`GET /api/creators`, сериализаторы `created_by_name` = ФИО/логин), «Лицевой счёт» квитанций — select счетов (ресурсозависимые справочные колонки); «Месяц»/«Год» в квитанциях — selects (год от первого года записей, `GET /receipt_documents/periods`); кнопки «Просмотр»/«Редактировать» без зелёной заливки (акцент, Просмотр в цвете Редактировать), отключён tooltip сортировки antd (`showSorterTooltip:false`). |
| Сентябрь 2026 | **«Приход/Расход»: запрет отрицательных сумм + названия документов по формуле** — сервер `_require_positive_document_amount` (сумма > 0 при create/update, 422) и правило формы; уменьшение суммы — правкой документа или противоположным. `renumber_transactions_chronological.py` после перенумерации пересчитывает названия «Тип операции №<id> от <дд.мм.гггг>» (№ = id), применено к 2114 документам. |
| Сентябрь 2026 | **2.3+Б15: движения по л/с, период и PDF выписки** — эндпоинты `GET /api/me/movements`, `/api/accounts/{id}/movements` (+`from_date`/`to_date`; строки `accounts_register`: Начисление/Оплата/Списание, услуга, основание, сумма, баланс) и PDF (`/me/statement/pdf`, `/accounts/{id}/statement/pdf` — детализация движений, `/reports/statement/pdf` — помесячно) через новый `backend/statement_pdf.py` (reportlab, шрифты из `receipt_config`). Фронт: блок «Движения по счёту» в `CabinetView` (ЛК жителя и просмотр администратора, RangePicker по периоду, кнопка «Выписка PDF»), период + PDF в `StatementReport.tsx`. |
| Сентябрь 2026 | **Квитанции: показания Пред./Текущ. в просмотре и PDF** — в `receipt_items` показания были пустыми: дозаполнены 1745 строк из `accruals_register` (UPDATE по (лицевой счёт, услуга, период); замена счётчиков учтена подбором пары с разностью = consumption). Теперь «Просмотр записи» квитанции и PDF показывают столбцы «Пред.»/«Текущ.»; новые квитанции пишут показания при генерации. |
| Сентябрь 2026 | **ЛК: знаки в «Движениях по счёту» для жителя + вёрстка PDF-квитанций** — в `CabinetView` суммы инвертированы визуально (начисление «−» красным, приход/списание «+» зелёным; логика расчётов не меняется), убран показатель «Внесено на счёт» (остались Начислено/Списано/Долг по 3 колонки). PDF-квитанции (`receipts.py`): шрифт таблицы 7pt (было 8), боковые отступы ячеек 3/2, ширины числовых колонок по содержимому, остаток листа отдан колонке «Услуга» — длинные «Электричество охраны»/«Итого Долг» помещаются в ячейки. |
| Сентябрь 2026 | **Роадмап: задача 2.18** — зафиксирована заявка владельца «Разовые и регулярные тарифы: не должно быть двух тарифов по одному виду услуги за один месяц» (сейчас показывается регулярный, а разовый уходит в долг). |
| Сентябрь 2026 | **PDF-экспорт отчётов (2.16)** — `backend/statement_pdf.py`: общий рендер `_report_pdf` (+перенос таблиц на всю ширину, «по содержимому», главные суммы справа) и `build_cash_register_report_pdf / build_expense_report_pdf / build_debtors_report_pdf`; эндпоинты `GET /reports/{cash_register,expenses,debtors}/pdf` (`backend/routers/reports.py`). Кнопки «PDF» на страницах `CashReport/ExpenseReport/DebtorsReport` через `openAuthorizedPdf`. Ориентация: касса/расходы — альбомная, должники — портрет; период/названия и сортировка должников по квартире. |
| Сентябрь 2026 | **Примечание квитанции (Б6)** — `receipt_documents.comment` (String 500, миграция `0016_receipt_comment`), сериализатор и колонка «Примечание»; `PATCH /receipt_documents/{id}/comment` (только comment, аудит); в `ReceiptViewModal` примечание читаемое, редактируется через кнопку «Редактировать» (по правам); при формировании («Квитанции за период») поле «Примечание для всех квитанций» попадает в созданные квитанции (`/receipt_documents/generate` принимает `comment`). |
| Сентябрь 2026 | **Выписка по л/с = как в ЛК + период по умолчанию текущий месяц** — `GET /reports/statement/pdf` и экран `StatementReport` переведены с помесячной сводки на движения по счёту (`build_account_movements` + `build_movements_pdf`, как в ЛК); период по умолчанию (текущий месяц 1-..-конец) в «Движениях по счёту» ЛК, отчётах по кассе/расходам и экране выписки. |
| Сентябрь 2026 | **Сортировка колонок (2.17) и расширения** — `GenericList`: `sortDirections=['ascend','descend']` (сортировка только возр./убыв., без сброса); в `sortMapping` добавлены `issued_at` (квитанции «Дата документа») и `contractor.full_name` (Приход/Расход «Контрагент»). Просмотр записи (`RecordFormModal`): reference-поля показывают текст связанной записи (`*_name`/вложенный объект), а не `id`. |
| Сентябрь 2026 | **Выравнивание колонок списков + тесты форматов (3.2, часть)** — `GenericList`: заголовки любых колонок по центру; значения ячеек по правилу «деньги → справа / текст → слева / остальное → по центру» (новый `config/columnAlign.ts`; в таблице стили выставляются на `th`/`td`, денежный текст уже даёт `formatMoney` — 2 знака, без `₸`). Тесты: vitest + RTL добавлены в devDependencies и скрипт `test` (`vitest.config.ts`, `src/test/setup.ts`); юнит-тесты правил (`config/columnAlign.test.ts`) и RTL на реальном списке «Приход/Расход» (`pages/GenericList.test.tsx`, мок Refine data provider): центр заголовков, справа/слева/центр значений, запятая и отсутствие `₸` у денег, дата `DD.MM.YYYY`. |
| Сентябрь 2026 | **Просмотр записи: фон и цвет полей (зафиксировано)** — в `RecordFormModal` (readonly-режим) disabled-поля: фон `#f5f5f5`, шрифт `#1f1f1f` через класс `.form-view-mode` в `App.css`; даты отображаются `DD.MM.YYYY` (`formatDate`, без сдвига по ТЗ). Шаблон — просмотр «Приход/Расход». Осталось покрыть тестами (см. ROADMAP 3.2). |
| Сентябрь 2026 | **В ЛК/админ-просмотре: блок «Расходы по кассе»** — `GET /me/house_expenses?from_date&to_date` (расходы по кассе, статьи+итог, из `cash_register`, без персональных операций) и блок в `CabinetView` (RangePicker периода, по умолчанию текущий месяц). Показывается и жителю (ЛК), и администратору в разделе «Личный кабинет жителя» (Администрирование); блок включается общим флагом `houseExpenses`, название без «(ТСЖ)». |
| Сентябрь 2026 | **Доработки интерфейса (08.09)** — (1) «Название документа» в просмотре документов показаний/начислений (`BulkReadingsModal`, `AccrualsCalculationModal`) теперь `disabled` в readonly-режиме (общее правило: в просмотре — без редактирования); (2) в `RecordFormModal.fieldViewText` общий fallback на лейбл вложенного объекта (тар. «Вида услуги» и др. reference–полей вида `services_type_id`) — показан текст вместо числового id; (3) `Users` приведён к конвенции списков (без столбца «Действия» в строках): выделение строки radio, панель действий «Редактировать»/«Удалить» (иконки+Tooltip, danger для удаления, placeholder disabled), «Просмотр» у пользователей не предусмотрен; (4) поля-периода в `CashReport`/`ExpenseReport` получили `format="DD.MM.YYYY"` (как у «Выписки по л/с»); (5) «Отчёт по должникам»: выбор даты среза `as_of` — срез по `operation_date <= конец дня` в метриках (начислено/оплачено/`cash_register`), параметр на JSON+PDF эндпоинтах и подзаголовок «По состоянию на: DD.MM.YYYY» в PDF, DatePicker `DD.MM.YYYY` на фронте; обратная совместимость (`as_of=None` = «на сейчас», `test_reports` не сломан); (6) PDF: добавлен `Content-Length` во все PDF-ответы (reports/others/receipts); по нажатию «PDF» документ скачивается — `authedFetch`-blob → <a download href=blob:…> в текущей странице (`downloadAuthorizedPdf` = `openAuthorizedPdf`), надёжно и на iOS. Язык: правило «всегда на русском» добавлено в глобальную инструкцию, навык `engineering-principles`, шаблон и проект-AGENTS. Доп.: «Просмотр квитанции» в ЛК на мобильном — таблица получила горизонтальную прокрутку внутри карточки (`scroll={{x:1000}}`, `max-width:640` укорачивает паддинг карточки), непрозрачные фоны строк и контрастный тёмный текст в `.receipt-preview` (значения не просвечивают); blob-скачивание PDF дополнено таймаутом (AbortController 60 с), обработкой сетевых ошибок и проверкой содержимого (не-PDF не качается как «мусор») — без `window.open`/`about:blank`. Затипизировано (tsc по затронутым файлам чисто), vitest 13 passed, `vite build` успешно. |
| Сентябрь 2026 | **Dev-пункт: проверка форматов после изменений интерфейса + «проваливание» в ссылки** — при добавлении/правке колонок, полей, форм и отчётов сверяется краткий чек-лист форматирования (заголовки по центру; денежные ячейки справа — 2 знака, запятая, без `₸`; текст слева / прочее по центру; даты `DD.MM.YYYY`; форма «Просмотр записи» — по шаблону «Приход/Расход»); канон и чек-лист: `~/projects/AI/principles/ui.md` (+ навык `table-formatting`, + строка в шаблоне `AGENTS.md` новых проектов). Зафиксирован в ROADMAP 2.21 запрос на переход («проваливание») из ссылочного поля/колонки в запись-источник (кнопка «…») — к реализации в отдельной задаче. |
| Сентябрь 2026 | **2.18 (08.09.2026): единая лента тарифов по срокам действия; снят `is_oneoff`** — тариф = ставка + `valid_from..valid_to`; признак разовости убран из модели/сериализаторов/UI. Миграции: `0017_tariff_period` (`valid_to` аддитивно) → `0018_tariff_period_conversion` (массовые разовые повышения месяца сведены в месячные каноны по (услуга, месяц); единственный Тип B — март-2024 «Фонда» → единая ставка 9560) → `0019_drop_tariff_oneoff`. Бэкенд: `Tariff.valid_to`; `resolve_tariff_for_accrual_period` (закрытый период → открытая база без `is_oneoff`), `apply_month_tariff_drafts` (ставки месяца при начислении), `retire_tariff_predecessors`/`validate_tariff_invariants` по виду/периоду; персональное доначисление `POST /accrual_documents/personal` (единица №4, вариант). Фронт: тарифы месяца в модалке начисления, форма «Персональное доначисление», колонка «Действует до» (`valid_to`) вместо «Периодичность», теги «Разовый/Регулярный» убраны. Контроль (песочница и живая БД на `0019`): monthly 10 132 / 55 915 776,00; oneoff (перс.) 30 / 4 092 716,00; итог **60 008 492,00**; pytest 50 passed; фронт `npm run build` ok. |
| Сентябрь 2026 | **Начисления: «Разовые» → «Персональные» + восстановлены комментарии (08.09.2026)** — с учётом снятия разовых тарифов подписи персональных документов (`doc_kind='oneoff'`) переименованы в UI (`OneOffAccrualsEditModal`, колонка/фильтр «Тип», сообщения backend): «Разовые/персональные» → «Персональные». В локальную «боевую» БД из исходника (`templates/Миграция данных FTH.xlsx`) дополнены пустые `comment` персональных/стартовых начислений по сопоставлению (напр. «2000 за аудит дымоходов» за янв-2019, «За асфальт и арыки», «66500 долг за парковку», «Входящий остаток» и др.). Отдельный дамп после внесения — `backend/backups/townhouse_0019_20260908_185209.dump`. |
| Сентябрь 2026 | **CI/CD и GitHub-чистка (08.09.2026)** — серверов `staging`/`production` нет (эксплуатация локальная, между ПК — дампами); удалены `deploy.yml`/`.github/actions/deploy`, в `ci.yml`/`docker-build.yml` остаются автопроверка и публикация образов; в GitHub вычищены окружения `staging`/`production` и deployment-записи; из `backend/backups/` оставлены последние 3 дампа данных + актуальные роли (старые `townhouse_2026*` за 06.09 удалены). ROADMAP/DEPLOY актуализированы, тег `v0.0.1` отозван (релизов не публиковалось). |
| Сентябрь 2026 | **2.19: Дашборд главной страницы (08.09.2026)** — новая страница-дашборд для бухгалтерских ролей (admin/operator/cashier/auditor) + домашняя страница после входа (`/`). Бэкенд `GET /api/dashboard` (`backend/routers/dashboard.py`, подключён в `app.py` до catch-all): единый срез «на сейчас» без параметров; потоки (Начислено / Внесено в кассу / Списано) — за последние 30 дней по `operation_date` строк `accounts_register`/`cash_register`, состояния (Долг / Остаток в кассе) — кумулятивно сегодня (формулы совпадают с отчётами должников и кассы; «Списано» = погашение долга по услугам). Ответ: `metrics`, `top_debtors` (топ-5), `debt_dynamics` (12 мес), `expenses` (расход кассы за 30 дн по статьям). Фронт `frontend/src/pages/Dashboard.tsx` (KPI-карточки через `Statistic`, блок «Должники» с переходом в `/debtors_report`, лента-таблица «Динамика долга», карточка «Расходы по кассе»); категория «Главная» → пункт «Дашборд» (`config/menu.ts`), роль в `auth/menuAccess.ts`, маршрут в `App.tsx`. Тесты `backend/tests/test_dashboard.py` (вкл. инвариант «долг сейчас == последняя точка динамики»), pytest новых — 5 passed; фронт `vite build` и `tsc` OK. ROADMAP: 2.19 закрыта. |
| Сентябрь 2026 | **Данные: восстановление «потерянных» начислений в `accounts_register` (вариант А, 08.09.2026)** — найдено и устранено: в первичном `accruals_register` лежали строки начислений (всего **165**, ~6 967 647 ₸), у которых **не было записи** в производном `accounts_register` (нет `accrual_id`) — поэтому они не попадали в срез взаиморасчётов и не влияли на долг; плюс 17 связей по марту‑2024 (Фонд, единая ставка 9560) были записаны в срез как `income=2000` (несоотв. `amount`). Следствиедо: занижённый долг по всем 17 л/с до переплат/нуля (например, кв.3 показывала 0 вместо долга **160 765 ₸**). Диагностика: `check_register_integrity` пометил все 17 л/с несогласованными (Δ начислений ровно 425 951 на счёт), контроль-тест с файл-источником по суммам при этом проходил. Исправлено точечно (`backend/migrations/restore_missing_accruals_to_accounts.py`, идемпотентный dry-run/`--apply`): недостающие начисления внесены в `accounts_register` (op=дата начисления, `services_type_id`, income=amount), мартовские 2024-связи приведены к полной сумме, `balance_after` пересчитан; существующие распределения/списания не перестраивались (сохранена история). Результат: «целостность» всех 17 л/с `consistent=True`, долг по дому **1 319 589 ₸** / 17 должников, кв.3 = 160 765 ₸; суммы начислений и кассы по-прежнему сходятся с файл-источником (единственное осознанное отличие — число строк начислений марта‑2024: 10179 в файле vs 10162 в БД, вызвано свёрткой 0018). Бэкап до изменений: `backend/backups/restore_variant_a_20260908_194613.dump`. |
| Сентябрь 2026 | **Данные: комментарий к документу начислений за март-2024 «Проверка газа - 7 560 тенге» + правило перепроводки (08.09.2026)** — на `AccrualDocument` (шапка «Начисление за март 2024», id 98, 101 строка по 17 л/с) внесён комментарий `comment='Проверка газа - 7 560 тенге'` с аудитом (`change_description='добавлен комментарий к начислениям март-2024'`, `updated_at`); регистры затронутых счетов перепроведены (`recalculate_register_balance`), несоответствий нет. Обоснование из исходника: в `templates/Миграция данных FTH.xlsx` за март-2024 по каждой квартире шли две строки — «Фонд развития» 2 000 (без комментария) и «Прочие расходы» 7 560 с комментарием **«Проверка газа»**; после чистки «Прочие расходы» обе сложены в «Фонд» 9 560 и свернуты в одну строку. Дополнительно: в `AGENTS.md` закреплено правило работы с документами/регистрами — любое изменение документа (начисления/показания/квитанции/приход-расход), влияющее на регистры: обязательный комментарий у шапки/операции + аудит + «перепроводка» регистров со сверкой целостности; суммы/строки регистров вручную не менять/не склеивать. |
| Сентябрь 2026 | **Тест согласованности «пересчёт начислений по тарифам» vs регистр (08.09.2026)** — новый `backend/tests/test_accruals_vs_tariffs.py` (read-only): для регулярных (`monthly`) начислений сумма должна выводиться из применяемого тарифа по типу услуги («Фиксированный»=ставке; «По площади»=ставке×площадь; «По счетчику»=ставке×consumption) — расхождений 0 по данным; разово-персональные (`oneoff`, входящие остатки/доначисления) хранят вручную введённые суммы и исключены из сверки. Отдельно закреплено: март-2024 «Фонд развития» — активный закрытый тариф-период №268 price **9560 ₸** на [2024-03-01..31] (ранее прогляднев), начисления за март-2024 по «Фонду» равны 9560. Итог по всей истории: регулярные начисления сходятся на 100%, единственные отклонения по «Фонду» — осознанные разовые/персональные документы с собственными комментариями причины. pytest — 2 passed. |
| Сентябрь 2026 | **Рабочий статус доработки вынесен в `.ai/PLANS/2026-dashboard-consistency-work.md` (08.09.2026)** — точка остановки сессии: готово (дашборд 2.19, восстановление долга accounts_register, комментарий март-2024, тест по тарифам, аннотации тарифов конверсии 2.18) и открыто (проектирование учета `opened_at` по правилу «не-по-всем = перс»). Смотрите также подраздел «Открытое разработка» в этом файле плана. |
| Сентябрь 2026 | **«Обнови описания» за 08.09.2026 (консолидация)** — по команде актуализированы и сведены все три файла: `ROADMAP.md` (2.19 закрыта → «Статус уже сделанного»; приоритеты обновлены; правки текста заметок), `PROJECT_STRUCTURE.md` (журнал изменений + фиксатор плана) и `DEPLOY.md` (сверено: инфраструктура/процедуры деплоя не менялись, правки не требуются). Незакрытая работа по учёту `opened_at`/перс-начислений документирована в `.ai/PLANS/2026-dashboard-consistency-work.md` — продолжать с неё. |
| Сентябрь 2026 | **Данные: восстановлены начисления «Вызов мусора» по кв13 (09.09)** — сверка 6 контрольных срезов (месяц / месяц×услуга / месяц×квартира / итого / итог×усл / итог×кв) файла-источника с БД выявила единое расхождение «Вызова мусора» кв13 (Δ 114 500; 74 мес 2020-07..2026-08 отсутствовали в БД, при этом в месячные доки «есть»). Миграция `backend/migrations/recover_kv13_garbage_accruals.py` (dry/--apply, идемпотентна) вставила 74 строки начисления (док+income в accounts_register, пересчёт). Контроль: все 6 срезов Δ=0 (общая 60 122 992), кв13 `check_register_integrity` consistent, кв13 долг 122 496, долг по дому 1 434 089 / 17 л/с. Долг по дому раст с корректным учетом ранее пропавших ечщений. |
| Сентябрь 2026 | **Начисления: `accounts.opened_at` + учёт в месячном расчёте (открытая разработка, Шаги 1–2, 09.09)** — Alembic `0020_account_opened_at` (accounts.opened_at DATE; бэкафилл = MIN(accrual_date) по л/с; по данным 15 л/с=2017‑10, кв8=2017‑11, кв4=2017‑12). В `backend/services.py` добавлен `_account_open_for_period` и фильтр в `calculate_accruals_preview` / `build_accrual_register_items` — в месячный начисляемый месяц попадает только л/с с opened_at <= конца месяца (вариант A/вперёд; правило не‑по‑всем = перс). Тест `test_accruals_preview_skips_accounts_later_opened`. Проверено в backend‑контейнере: файл 3 passed; полный набор 55 passed (3 фейла «шум» test_filtering на живой БД). Продолжение: Шаги 3–5 плана.
| Сентябрь 2026 | **Начисления: конвенция перс-документов + объединён «аудит дымоходов» янв-2019 (09.09)** — перс-корректировка одного вида на несколько л/с ведётся ОДНИМ документом (как перс-сбор 2022-01). «Аудит дымоходов» 2019-01 объединён из 8 oneoff (docs 22–29) в единый перс-документ id=135 (8×2 000; миграция `backend/migrations/merge_2019_01_audit_personal.py`) — доход/accounts не тронуты, docs 22–29 удалены; сумма месяца 526 150 ₸ и итог по дому 1 434 089 ₸ сохранены. По сверке с исходником: 2017-12 и 2018-04 перс-досборы кв4 (350 000+168 000 и 191 000) уже отдельные oneoff — соответствуют файлу; 2020-06/кв13 «Корректировка за парковку» −15 000 — перс-корректировка (±). Бэкап: `backend/backups/merge_2019_audit_pre_20260909.dump`.
| Сентябрь 2026 | **Начисления: AccrualsSumCheck — сверка файла и БД по методике (09.09)** — новый модуль `backend/migrations/accruals_sum_check.py` и тест `backend/tests/test_accruals_sums_method.py`: 6 срезов (месяц / месяц×услуга / месяц×квартира / итог / итог×квартира / итог×услуга) сверяют суммы начислений файла-источника с БД по «Период_Начисление» accrue_date и канонических видов услуг (Прочие→Фонд; охрана-эл). Нулевые строки источника не учитываются. Файл/БД импортированы отсутствуют — тест пропускается (CI/dev пустых). Проверено на живой dev и после всех реклассификаций: **все срезы совпадают**, итог 60 122 992 ₸. Независимый CLI (read-only) для миграции в прод: `python migrations/accruals_sum_check.py`. |
| Сентябрь 2026 | **Начисления: реклассификация 2020‑02 и 2024‑10 (monthly/перс, 09.09)** — продолжение Шага 3 того же паттерна: месячные Фонды дополнены до «на всех, кто открыт», и перс-компоненты вынесены в свои oneoff; нагрузка л/с не меняется. (а) **2020‑02**: месячный док 43 дополнен Фондом 10 000 кв2 и кв12 (их строки были свёрнуты в перс. docs 44/45), перс понижены doc44 76 500→**66 500**, doc45 32 000→**22 000** («долг за парковку» — перс). (б) **2024‑10**: месячный doc 106 дополнен Фондом 2 000 кв5 (был в перс. doc 107), перс doc107 14 000→**12 000** («ремонт воды»). Миграция `backend/migrations/reclassify_2020_02_2024_10_fond_personal.py` (dry/`--apply`, идемпотентна; комментарии + аудит `migration(id=1)`; accounts_register пересчитан). Контроль: итог по дому 1 434 089 ₸ сохранён; сумма 2020‑02 = 616 450 ₸, 2024‑10 = 451 435 ₸; повторный dry‑run = 0. Бэкап до правок: `backend/backups/reclassify_2020_2024_pre_20260909.dump`. |
| Сентябрь 2026 | **Начисления: реклассификация 2019‑01 и 2022‑01 (monthly/перс) (09.09)** — по правилам «не‑по‑всем = перс» и решению владельца: (а) **2019‑01** — месячный Фонд 10 000 восстановлен на все 17 л/с (doc 21 получил строки кв 4,7,9,11,12,13,14,17, которых в месячном не было — они лежали внутри oneoff‑22…29 по 12 000), а oneoff понижены до **2 000** («аудит дымоходов» остаётся перс.): нагрузка каждого л/с не меняется (12 000 ⇄ 10 000+2 000); (б) **2022‑01** — спец‑сбор «парковка/освещение» 6 000 на 13 л/с (из «Прочие расходы») вынесен из месячного doc 71 в отдельный перс‑документ `oneoff` id=134 (13 строк ×6000); месячный Фонд в январе‑2022 отсутствует. Миграция `backend/migrations/reclassify_2019_01_2022_01_personal.py` (dry/`--apply`, идемпотентна; комментарии + `updated_by=migration(id=1)` + `change_description`; accounts_register пересчитан). Контроль: итог по дому 1 434 089 ₸ сохранён; суммы месяца 2019‑01 = 526 150 ₸, 2022‑01 = 448 170 ₸; повторный dry‑run = 0. Бэкап до правок: `backend/backups/reclassify_2019_2022_pre_20260909.dump`. |
| Сентябрь 2026 | **Тесты сверки вынесены в `backend/tests/check_sum/` (09.09)** — контрольные тесты «файл-источник ↔ БД» сгруппированы в папке `backend/tests/check_sum` (README.md с описанием и запуском): `test_accruals_sums_method.py` (начисления, 6 срезов), `test_cash_slices_vs_source.py` (касс: приход/расход/по квартире по месяцам), `test_registers_settlements_vs_cash.py` (взаиморасчёты: итог/кв/месяц/месяц×кв), `test_cash_articles_pending.py` (xfail: «статьи кассы», ждёт трактовку). Σ эталоны: начисления 60 122 992 ₸, касса-приход 60 401 744 ₸, касса-расход 60 039 094 ₸, взаиморасчёты 58 688 903 ₸ — Δ=0, 8 passed + 1 xpass. |
| Сентябрь 2026 | **ТД-4: модель «переливов» + тест остатков по услугам; пересчитан счёт 13 (09.09.2026)** — разобрана несходимость «приход по Код_Аналитика файла ↔ `accounts_register.expense` по услуге»: прямого перс-сопоставления нет и не должно быть (платёж распределяется между услугами долга автоматически по `services_type.priority`, Фонд — последний — «переливы»). Новый тест `tests/check_sum/test_registers_services_vs_writeoff_model.py` (read-only): фактический `expense` по услугам (и остатки income−expense) должен воспроизводиться моделью — деньги счёта (`cash_register`) → долги услуг (`accruals_register`, сторно учтено) по приоритету. На живой БД выявил рассинхрон **счёта 13** (кв.13): начислен «Вывоз мусора» 114 500, `expense`=0 (деньги ушли «Фонду» при последнем пересчёте до появления начислений). Исправлено пересчётом `backend/migrations/resync_accounts_register_to_accruals.py` (dry/`--apply`, пересоздаёт accounts_register из первички штатным `rebuild_accounts_register`): «Вывоз мусора» → 114 500, «Фонд» 1 401 526→1 287 026; сумма списаний счёта не менялась (3 560 115), `balance_after` согласованы. Контроль: `check_sum` **13 passed + 1 xpass**, сверка с файлом по кв/месяцу/итогу Δ=0, долг по дому 1 434 089 ₸ сохранён. ROADMAP: ТД-4 закрыта (старый xfail `test_cash_articles_pending.py` оставлен как документирующий требование трактовки «статьи» для UI/отчётов). |
| Сентябрь 2026 | **2.22 (часть): ЛК — сокращение блоков по списку владельца (09.09.2026)** — `frontend/src/components/cabinet/CabinetView.tsx` (ЛК жителя и админ-просмотр счёта, логика расчётов не менялась): (1) из карточки «Лицевой счёт» убрана сводная таблица «Начислено/Внесено на счёт/Списано/Долг/Остаток на счёте» (эти данные ниже — в «Детализации по услугам» и «Движениях»); (2) «Детализация по услугам» — остались колонки «Услуга» и «Долг»; (3) «Движения по счёту» — период по умолчанию последние 30 дней (было: текущий месяц), удалён столбец «Основание», метрики переименованы в «Начислено за период»/«Оплачено за период»/«Долг на конец периода», на мобильном карточки метрик вертикально (`Col xs=24 md=8`), у таблицы скролл `x:max-content`; (4) «Мои квитанции» — без колонок «Квартира»/«Собственник», кнопка «Просмотр» убрана (осталась только PDF); (5) таблицы блоков ЛК выровнены по канону проекта через `config/columnAlign.ts` (`headerAlignStyle` на заголовках, `cellAlignStyle` на ячейках; добавлены ключи `service_name`/`service`/`kind_label`): заголовки по центру, деньги справа, текст слева; (6) блок «Расходы по кассе» переименован в **«Общедомовые расходы»**, перенесён в самый низ страницы, выбор периода убран — всегда за последние 30 дней (фронт, `GET /me/house_expenses`); (7) навигация жителя: `ProtectedLayout` (`App.tsx`) не рендерит `Sidebar` для роли `resident` (единственный раздел — кабинет), внизу `ResidentCabinet.tsx` — три кнопки «Главная» (navigate `/`), «Настройки» (заглушка: `message.info`) и «Выход» (`useLogout`). Фронт собран: `tsc` + `vite build` OK (в контейнере `townhouse-frontend`). ROADMAP: 2.22 — этап «применение» частично выполнен (осталось: проверить на реальном мобильном вьюпорте). |
| Сентябрь 2026 | **2.22 (продолжение, 09.09.2026): ЛК — вертикальные списки, единый период, мобильная навигация** — блоки ЛК переведены с таблиц на вертикальные списки с крупными суммами (общий `SUM_FONT` = `clamp(22px→30px)` жирный): «Квитанции» (месяц + сумма + квадратная кнопка PDF 53px по центру ячейки; период с запятой «Август, 2026»; фильтр по месяцу), «Услуги» (метрики «Начислено за период/Оплачено за период/Долг на конец периода» над списком долгов по услугам), «Деньги» (движения Регистра денежных средств: дата + сумма ±, колонка «Счёт/Касса» убрана, PDF внизу), «Общедомовые расходы» (статьи + отцентрированный между линиями итог). Убран `#root { max-width: 1280px }` шаблона Vite (`App.css`) — блоки на всю ширину страницы; шрифты ЛК крупнее (`ConfigProvider fontSize: 17`, вложен в `ResidentCabinet`); навигация жителя внизу страницы (кнопки 88×72, иконка над подписью); кнопки PDF квадратные 53px. **Единый период** в отдельном блоке под «Лицевым счётом» применяется ко всем блокам: пресеты «За 3 месяца» (по умолчанию), «За прошлый месяц», «За текущий месяц», «За период» (две даты, allowClear); пересчёт «Деньги» (движения + метрики) и «Общедомовых расходов» — на бэкенд (`/me/movements`, `/me/house_expenses` с датами), «Квитанции» — локальный фильтр по месяцу. Бэкенд: `build_account_movements` отдаёт `cash_movements` (строки `cash_register` по л/с: дата, касса/счёт, сумма). Проверено API (счёт 1, август 2026: `cash_movements` = 09.08 «Касса» 170 000). Фронт собран (`tsc`+`vite build` OK). Осталось (завтра): форматирование ЛК. |
| Сентябрь 2026 | **ЛК: форматирование по канону фронтенда (10.09.2026)** — `CabinetView` переведён на общие форматтеры `config/formatters.ts` (`formatMoney`/`formatDate`/`formatPeriod` вместо локальных `fmt`/`fmtDate`/`periodLabel`; `fmtSigned` теперь строится на `formatMoney`): единый денежный формат (2 знака, запятая, без ₸), даты `DD.MM.YYYY`, период квитанции — `formatPeriod` («Август, 2026 г.»). Денежные суммы в вертикальных списках выровнены вправо («Услуги», «Движения», «Квитанции»; «Общедомовые расходы» уже были справа); метрики `Statistic` получили `formatter={formatMoney}` (не зависят от locale antd). Убраны локальные копии `fmt` также в отчётах `StatementReport`/`CashReport`/`DebtorsReport`/`ExpenseReport` (→ общий `formatMoney`). Валидация: `tsc` чисто, vitest 13 passed, `vite build` OK. |
| Сентябрь 2026 | **ЛК: единый вид и наполнение блока «Движения» (11.09.2026)** — (1) в `CabinetView` введены константы стиля строк `ROW_LABEL` (17px, вес 400) и `ROW_TOTAL_LABEL` (17px, 600); все подписи внутри блоков («Услуги», «Движения», «Квитанции», статьи «Общедомовых расходов») приведены к ОДНОМУ виду — как в отчёте «Общедомовые расходы», без «полужирного» акцента и разных кеглей; кегль сумм уменьшен (`SUM_FONT` = `clamp(17px, 4vw, 22px)`, вес 600). (2) Блок **«Деньги» переименован в «Движения»** и больше не пустует в месяцах без кассовых платежей: показывает движения по счёту (`movements`: начисление/оплата/списание, дата + вид + услуга), итог «Внесено в кассу за период» и расшифровку кассовых операций (`cash_movements`) мелким шрифтом; знаки для счёта выправлены (начисление увеличивает долг → красное `+`, оплата/списание → зелёное `−`). Добавлен тест `src/components/cabinet/CabinetView.test.tsx` (3 кейса: содержимое блока, единый стиль подписей, денежный формат). Валидация: `tsc`/`vite build` OK, vitest 16 passed. |
| Сентябрь 2026 | **Доставка прод-образов: исправлен `docker-build.yml` (11.09.2026)** — обнаружено, что workflow собирал фронтенд из **dev**-`frontend/Dockerfile` (`CMD npm run dev`, Vite:5173), т.е. тег `v1.0.0` опубликовал бы образ, несовместимый с NAS (`docker-compose.prod.yml` ждёт nginx:80) — приложение бы не запустилось. Matrix переведён на явный `dockerfile` (`backend/Dockerfile`, `frontend/Dockerfile.prod`), сборка идёт по `./${{ matrix.dockerfile }}`. Дополнительно в `docker-compose.prod.yml` убран дефолт `${TOWNHOUSE_TAG:-main}` → `${TOWNHOUSE_TAG:?...}`: без явного тега compose отказывается стартовать (защита от dev-образа из `main`). DEPLOY §7.6/§10.1 актуализированы. |
| Сентябрь 2026 | **CI: проверка сборки прод-образов (11.09.2026)** — в `ci.yml` добавлен job `images`: сборка `frontend/Dockerfile.prod` и `backend/Dockerfile` без публикации (cache gha) + **smoke-тест** фронтенд-контейнера (старт `docker run`, `/` и глубокий маршрут → 200, контейнер обязан быть `running`). Востребовано после того, как прод-образ дважды ломался незаметно: `npm ci` падал из-за прав `/app` при `USER node`, а nginx падал на старте с «host not found in upstream backend» без рантайм-resolver (лечится `resolver 127.0.0.11` + `proxy_pass` через переменную). Существующий job `frontend` (`npm run build` на хосте) этих классов ошибок не ловил. Проверено локально реальными командами: оба образа собираются, backend отвечает 401/200, фронт-контейнер отдаёт SPA. В branch protection добавить `CI / Images …`. |
| Сентябрь 2026 | **Прод-развёртывание на NAS Synology: 3-контейнерный стек + перенумерация БД (ТД-2) (11.09.2026)** — подготовлен прод-контур: `docker-compose.prod.yml` (3 контейнера: `postgres:16-alpine` с volume и healthcheck по `${POSTGRES_*}`, `backend` из ghcr без монтирования кода, `frontend` наружу через `APP_PORT`→80; HTTPS — обратный прокси DSM на `fth.sagacloud.synology.me`); `frontend/Dockerfile.prod` (multi-stage `npm run build` → `nginx:alpine`) + `frontend/nginx-container.conf` (SPA `try_files`, `/api/` → `backend:8000`, кэш ассетов); `.dockerignore` для обоих контекстов (дампы/тесты не попадают в образы, `assets/FTH.png` сохраняется); `deploy/.env.prod.example` (`TOWNHOUSE_TAG=v1.0.0`, секреты, `CORS_ORIGINS`). Перенос живой БД — с полной перенумерацией (вариант B/ТД-2): новые скрипты `backend/migrations/renumber_all_entities.py` (id всех таблиц → 1..N без дырок в порядке зависимостей, перепривязка всех FK + `setval`; dry-run/`--apply`; на живой dev-БД нашёл 226 дырок в тарифах, 985 в users, 605 в receipt_items) и `renumber_finalize.py` (хронология начислений и «Приход/Расход» — «Начальный остаток» → id=1, пересборка `accounts_register`, перегенерация квитанций, контроль `check_register_integrity`). Старые частные скрипты (`recreate_and_resync.py`, `renumber_transactions_chronological.py`, `reorder_receipts_by_period_apartment.py`) помечены устаревшими в `migrations/README.md`. Порядок переноса — `DEPLOY.md` §7.6 (дамп → схема → renumber --apply → finalize → контрольные суммы → админ). Учтён риск: перенумерация `users` сбрасывает выданные JWT (`sub=user.id`). |
| Сентябрь 2026 | **Брендинг: favicon «Family Townhouse» (11.09.2026)** — на развёрнутом NAS-контуре (`fth.sagacloud.synology.me`) вкладка показывала дефолтный логотип Vite: в `frontend/index.html` иконка указывала на `/vite.svg`, а собственный логотип проекта (`frontend/public/FTH.png`) не был подключён ни в одной точке. Из `FTH.png` (73×90, логотип на белом фоне) сгенерирован набор иконок: `frontend/public/favicon.ico` (multi-size 16/32/48), `favicon-16x16.png`, `favicon-32x32.png`, `apple-touch-icon.png` (180×180); изображение выровнено по квадрату с белым фоном (как в оригинале). В `index.html` ссылка на `vite.svg` заменена на этот набор (+ `theme-color`), неиспользуемый `public/vite.svg` удалён; `FTH.png` оставлен как источник (пригодится для `og:image`). Nginx-конфиг/деплой не менялись — статика из `public/` попадает в `dist/` при `vite build`. Проверка: прод-образ собран по `frontend/Dockerfile.prod`, иконки в образе, все четыре URL (`/favicon.ico`, `/favicon-32x32.png`, `/favicon-16x16.png`, `/apple-touch-icon.png`) отдаются nginx с **200** и корректным `Content-Type` (`image/x-icon`/`image/png`). Доставка на NAS — новым релизным тегом через `docker-build.yml` (см. DEPLOY §7.6); в браузере после обновления обязателен hard-refresh (favicon кэшируется отдельно и упорно). |
| Сентябрь 2026 | **Dev-среда: устранён сбой Vite «Outdated Optimize Dep» (11.09.2026)** — причина ложных симптомов «блок/страница не обновляется»: анонимный том `/app/node_modules` в `townhouse-frontend` был неполным (без `vitest` и др.), из-за чего при каждом старте Vite писал `Re-optimizing dependencies because lockfile has changed`, предбандл не собирался, и запросы к `/node_modules/.vite/deps/*` (напр. `@dnd-kit/core`) отдавали `504`, валя импорт страницы. Лечение: `docker compose restart frontend` (при необходимости `npm install` в контейнере или пересоздание тома). В `DEPLOY.md` §7.2 добавлен подраздел с симптомом, командами и проверкой (200 vs 504); там же — пример `curl` к `/api/me/movements` для разграничения «данных нет» и «страница не отрисовалась». |
