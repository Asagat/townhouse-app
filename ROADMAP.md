# Roadmap — Townhouse ERP

Сводный план развития проекта. Дата составления: август 2026. Все изменения фиксируются в git (ветка `main`), каждое направление — отдельными коммитами.

**Правило ведения:** как только задача выполнена, переноси её детали в раздел
«Статус уже сделанного (справочно)» внизу и **полностью удаляй** соответствующую строку
из планового раздела (разделы 2/3/Бэклог/Идеи) — без строки-указателя или ссылки.

## Что осталось сделать (ввод CI/CD в строй; код готов — нужна настройка GitHub и сервера)

> По задаче 3.3: каркас в репозитории, GitHub-часть выполнена частично (09.2026):
> окружения `staging`/`production` созданы, секрет `SSH_KEY` добавлен, branch protection
> с required checks на `main` включён. Где будут тестовый и продовый серверы — решение
> отложено; памятка с чего продолжить — DEPLOY.md §10.6.

- [x] Окружения `staging`/`production` созданы; секрет `SSH_KEY` добавлен (09.2026);
- [x] Branch protection для `main`: required checks `CI / Backend…`, `CI / Frontend…` (09.2026);
- [ ] Определить, **где** будут `staging` и `production`, и способ доставки (SSH или self-hosted runner на corsus) — отложено (см. DEPLOY.md §10.6);
- [ ] Секреты `SSH_HOST`/`SSH_USER` (+`APP_DIR`) в окружениях — после выбора серверов;
- [ ] Публичный SSH-ключ — в `~/.ssh/authorized_keys` на каждом целевом сервере;
- [ ] (опц.) Required reviewers для окружения `production`;
- [ ] Тестовый (staging) сервер для сверки данных + первый деплой из Actions.

---

> **Заметки (сентябрь 2026)**
> - **07.09.2026 — формы и списки: денежные/телефонные/датовые форматы, справочные фильтры, кнопки**: (1) **деньги** — единый формат с разделителями разрядов и 2 знаками (`formatMoney`) в колонках списков и во всех формах (ввод `moneyInputFormatter`/`parser`, просмотр); (2) **телефон** — контроль ввода (цифры, «8»→«7») + маска «+7(XXX)XXX-XX-XX» при потере фокуса/в просмотре (`normalizePhone`/`formatPhone`, `PhoneInput`; нейтральный placeholder); (3) **просмотр записей** (`RecordFormModal`): readonly-поля больше не привязаны к хранилищу формы (раньше `Form.Item` подменял текст на dayjs/число — отсюда «Wed, 31 Dec…» у дат), единый `fieldViewText`: даты `DD.MM.YYYY` (без сдвига по ТЗ), `datetime` с временем, булево «Да/Нет», деньги, телефон; фон disabled-полей серый (как «Просмотр показаний»); (4) **фильтры** — «Автор» select из пользователей (`GET /api/creators`; сериализаторы: `created_by_name` = ФИО или логин), «Лицевой счёт» квитанций — select из справочника счетов (ресурсозависимые справочные колонки); в «Квитанциях» месяц — списком названий, «Год» — динамический select от первого года записей (`GET /receipt_documents/periods`), колонка «Месяц» — названием; (5) кнопки «Просмотр»/«Редактировать» без зелёной заливки (белый фон + акцент, Просмотр в цвете Редактировать), отключён всплывающий tooltip сортировки antd (`showSorterTooltip:false`), перекрывавший панель действий; «Показание» в просмотре документа показаний — с разделителями разрядов без дробной части.
> - **07.09.2026 — «Приход/Расход»: запрет отрицательных сумм и названия документов по формуле**: создание/редактирование документа с суммой ≤ 0 → 422 (сервер `_require_positive_document_amount`) и правило в форме; уменьшение суммы — правкой документа или противоположным документом. `renumber_transactions_chronological.py` дополнительно пересчитывает **названия** документов «Тип операции №<id> от <дд.мм.гггг>» (№ = id после перенумерации); применено к 2114 документам (расхождений 0).
> - **07.09.2026 — свёртка приходов на (л/с, день), отрицательных приходов больше нет** (данные): в старой базе не было лицевых счетов, переплаты относились на «Фонд развития» и при следующем взносе «снимались» отрицательными строками «Приход в кассу». `backend/migrations/reimport/collapse_income_by_day.py`: все приходы одного л/с за день сливаются в один документ (сумма = нетто дня, статья «Поступления от жителей»), дни с нетто ≤ 0 удаляются и печатаются списком «требуют разбора» (в данных таких нет); после — пересчёт `cash_register` и пересборка `accounts_register`; идемпотентен (dry-run/`--apply`). Применено к dev-БД: 486 групп, удалено 495 документов-доноров, отрицательных приходов 0 (было 485), приходы «Поступления от жителей» — 1 322 документа / 58 650 903 ₸; квитанции (1 816) не пересоздавались — нетто не менялось. Затем `renumber_transactions_chronological.py`: id/doc_no перенумерованы в хронологии (2 114 транзакций, «Начальный остаток» = №1). Бэкапы `backend/backups/townhouse_{precollapse,prerenumber}_*.sql`.
> - **07.09.2026 — ресайз ширины колонок (2.1)**: ручка на правом краю заголовка каждой колонки тянет ширину (собственная реализация на pointer-событиях: ширина = стартовая ширина заголовка + дельта движения указателя — работает при любом `scroll`, включая `x: 'max-content'`; библиотека не понадобилась). Ширины сохраняются в localStorage по (ресурс, роль) вместе с порядком/видимостью (`useColumnSettings` → `{order, hidden, widths}`). Ручка не конфликтует ни с drag&drop заголовков (dnd-kit), ни с сортировкой по клику. «Действия» не растягивается.
> - **07.09.2026 — Б10 доработки, статус тарифов (Б11), порядок колонок (2.10-C)**: (1) фильтры «содержит»/«равно» по **вложенным полям** (колонки-справочники) падали с 500 — в `filtering.py` тип столбца классифицировался дважды; исправлено. Колонки-справочники в списках (касса/счёт, статья, контрагент, собственник, л/с, квартира, вид услуги, счётчик, тип тарифа) теперь фильтруются **выпадающим списком со значениями из того же справочника** (`ReferenceFilterSelect`, кэш по ресурсу), а не текстом. «Регистр начислений» по умолчанию отсортирован по **«Периоду»** (свежие сверху) — раньше первыми шли «входящие остатки» 2017 (Фонд развития), из-за чего казалось, что показаний нет. (2) **Тарифы (Б11)**: статус тарифа «Действующий/Архивный» (`tariffs.status`, миграция `0014_tariff_status`; действующий — последний в группе «вид услуги + разовость», остальные — архивные); при создании нового тарифа предыдущие действующие той же группы автоматически становятся архивными (`services.retire_tariff_predecessors`; расчёт начислений от статуса не зависит); в «Тарифах» — колонка «Статус» и фильтр по статусу, раздел по умолчанию открывается на «Действующих» (в `GenericList` появились стартовые фильтры раздела и ресурсо-зависимые select-фильтры). (3) **Порядок колонок (2.10-C)**: drag&drop **самих заголовков колонок** и в панели «Отображаемые колонки» (`@dnd-kit/core+sortable+utilities`, `hooks/useColumnSettings.ts` — localStorage `{order, hidden}` по (ресурс, роль), старый формат видимости мигрируется, новые колонки конфига не теряются; «ID»/«Действия» закреплены; короткий клик = сортировка, сдвиг от 5px = перетаскивание). Также ранее: в «Начислениях» и «Регистре начислений» поле «Дата начисления» переименовано в «Период» и отображается как «Январь, 2025 г.». pytest 48 passed.
> - **06.09.2026 — «Тип тарифа» и «Ед. изм.» перенесены с тарифа на вид услуги**: привязки задаются один раз на «Виде услуги» и наследуются всеми тарифами (миграции `0012_service_tariff_type`, `0013_service_unit`; колонки `tariffs.tariff_type_id`/`tariffs.unit` удалены). Мотив: пользователь ошибочно указывал тип на часто вводимом тарифе → начисление не проходило; тариф вводится чаще, чем вид услуги. Расчёт начислений и валидация счётчиков читают тип услуги (`service_supports_meter`/`has_meter_tariff`); формы: «Тип тарифа»/«Ед. изм.» в «Видах услуг», из «Тарифов» убраны; сортировка тарифов по этим полям — через вид услуги. Контроль: начисление 08.2026 после переноса не изменилось (100 строк / 614 680 ₸ до и после; pytest 42 passed).
> - **06.09.2026 — развёртывание: HTTP-only и зеркальная синхронизация между ПК**: внешний nginx на домашнем ПК переведён на HTTP (:80), HTTPS-сертификат/ACME удалены (HTTPS — опционально через NAS Synology). DEPLOY §7.2–7.5: схемы ПК «corsus» (docker-стек) и «snowflake» (нативный), симметричный режим «разработка по очереди» (дампы передаются через Synology Drive); скрипты `dump_to_sync.sh`/`refresh_from_backups.sh`/`update_db_after_pull.sh` + git-hooks `post-merge` (автоимпорт после pull) и `pre-push` (автодамп перед push); у `restore_townhouse.sh` появился флаг `--yes`.
> - **06.09.2026 — настроен каркас CI/CD (п. 3.3)**: `.github/workflows/ci.yml` (backend: disposable `postgres:16` → `alembic upgrade head` → `init_data.py` → pytest; frontend: `npm ci` → `npm run build`), `docker-build.yml` (образы в ghcr.io на push в main и по тегу `v*`), `deploy.yml` + action `deploy` (выкат `scripts/deploy_vps.sh <ref>` по релизному тегу на прод или вручную на staging/production; гейт — успешный CI для коммита). `scripts/deploy_vps.sh` теперь умеет фиксировать ветку/тег/SHA и **пересобирает фронтенд** (раньше при обновлении статика nginx не обновлялась). Осталось вне репозитория: секреты SSH, окружения `staging`/`production`, required checks (см. DEPLOY.md §10). Тест-сверка с файлом-источником пропускается на пустой БД (disposable-БД CI) — см. ТД-3.
> - **06.09.2026 — новые заявки владельца** (зафиксированы в разделе 2 ниже): 2.13 (отдельное хранение настроек пользователя — сортировки/фильтры/порядок полей), 2.14 (разделение «Приходы/Расходы» на ПКО / РКО / платёжное поручение / поступление на счёт; состав меню зависит от типов кэшпоинтов — см. 2.15), 2.15 (поле «Тип» = «Счёт»/«Касса» в справочнике «Кассы/Счета»: есть кэшпоинт «Касса» → в меню ПКО и РКО; есть «Счёт» → платёжное поручение и поступление на счёт), 2.16 (экспорт всех отчётов в PDF), 2.17 (сортировка столбца — только «по возрастанию»/«по убыванию», без третьего состояния).
> - **06.09.2026 — реструктуризация роадмапа**: открытые задачи сгруппированы по категориям (Технический долг / UX и функциональность / Качество / Бэклог / Приоритеты / Идеи развития). Сверка по коду и данным: (1) исторический перенос «Контрагентов» на старые операции — **выполнен** (проверка dev-БД: 2609/2609 транзакций с `contractor_id`, все ссылки валидны; прод перезалит при сентябрьском накате); (2) выявлен **дефект PDF**: кнопки «PDF» открывают квитанцию в новой вкладке без токена → «Требуется авторизация» (см. ТД-1); (3) «Скачать (ZIP)» всех квитанций периода уже реализован (`bulk_pdf` + кнопка в `ReceiptsModal`) — из п. 2.4 остался только брендинг.
> - **06.09.2026 (вечер) — чистка справочника «Виды услуг» (по решению владельца)**: удалён дубль **«Электричество»** (каноническая — «Электроэнергия»; `migrations/dedupe_electricity_service.py`); услуги **«Прочие расходы» больше нет** — её 30 исторических начислений (206 520 ₸, парковка/освещение и проверка газа) переведены на «Фонд развития» с **разовыми тарифами 6000/7560 ₸** (`move_prochie_rashody_to_fond.py` + `fix_fond_oneoff_tariffs_for_prochie.py`; пересборка `accounts_register`, перегенерация квитанций; суммы по квартирам/итоги не изменились — контроль 60 008 492,00). В `init_data.py` из услуг по умолчанию убраны «Электричество»/«Холодная вода» (не плодить дубли поверх импортированного набора). В справочнике осталось **6 услуг**: Электроэнергия, Охрана, Охрана-электроэнергия, Обслуживание ТП, Вывоз мусора, Фонд развития.
> - Денежные сущности теперь несут **два независимых среза** для подробных отчётов:
>   - **«Статья»** (`transactions.article_id` → `analytic_articles`, статьи доходов/расходов) — тип движения денег; **«Контрагент»** (`transactions.contractor_id` → `counterparties`) — связанная сторона операции. Оба поля продублированы/доступны в «Регистре денежных средств» (контрагент — собственный столбец `cash_register.contractor_id`). Миграции Alembic: `0006_cash_service_link` и `0007_document_create_ts` применены к живой БД.
> - **«Дата документа» и «Дата создания»** разделены у всех документов. У «Приход/Расход» (`transactions`) и «Квитанций» (`receipt_documents`) заведена отдельная колонка `created_at` — миграция `0007_document_create_ts` применена к живой БД; существующие строки дохистории заполнены датой документа (`transaction_date`/`issued_at`). В UI у «Приход/Расход» и «Квитанций» выведены обе колонки («Дата документа» и «Дата создания»).
> - Справочники не смешивать: **«Статья»** (`analytic_articles`) — для «Приход/Расход»/кассы; **«Виды услуг»** (`services_type`) — для «Начислений»/счётчиков. Поле «Виды услуг» в денежных документах было введено и **отменено** (решение владельца): из «Приход/Расход» и «Регистра денежных средств» оно убрано и заменено на «Контрагент» (миграция `0008_cash_contractor`). Названия полей/колонок в UI приведены к именам справочников («Статья», «Контрагент»).
> - **Статьи доходов и расходов** — эталонный справочник задан (доходы: Поступления от жителей, Возвраты от контрагентов, Прочие доходы; расходы: Электроэнергия, Водоснабжение, Вывоз мусора и утилизация, Заработная плата персонала, Обслуживание инженерных систем, Благоустройство территории, Безопасность и проверки, Материалы и инвентарь, Возвраты жителям, Прочие расходы). Источник истины — `_ANALYTIC_ARTICLES` в `backend/init_data.py`; сидер `backend/seed_analytic_articles.py` добавляет недостающие строки (идемпотентно). В живой БД статьи внесены.
> - Переразнесение документов «Приход/Расход» на новые статьи **Выполнено** (миграция `migrate_expand_article_2026.py`, единственная транзакция): 778 операций-расходов с «Расходы по дому» разнесены по 9 статьям (сумма сходится 60 039 094, контроль печатается в скрипте). Проверено: ссылок на старую статью нет → статья «Расходы по дому» **удалена**; в справочнике осталось 13 строк (3 дохода + 10 расходов). «Статья» в регистрах (cash_register/accounts_register) не дублируется — она берётся из `transactions.article_id`, поэтому везде корректна автоматически.
> - Исторический перенос «Контрагентов» для существующих денежных операций — ~~отложено~~ **выполнен** при перезаливке истории (проверено 06.09.2026, см. первый пункт). Новые документы «Приход/Расход» уже позволяют указывать «Контрагент».
> - Тип тарифа **«По жильцам»** — отложено (вернуться позже, см. Б8). Не вводить в `tariff_types` и логику расчёта начислений до отдельного решения (нужны: источник количества жильцов в квартире и поддержка типа в `calculate_accrual_for_account_service`).
> - **Prod-накат завершён (сен. 2026):** ветка `feat/owners-to-counterparties` + миграции до `0011` применены к прод-БД `townhouse`; таблица `owners`переименована в `counterparties`, добавлены тип «Входящий остаток» и `transactions.doc_no`. История перезалита по контроль-сборке со `townhouse_stage`: касса 10 140 (расход 60 039 094), начисления 10 179, квитанции 1 816 (сумма 60 008 492), показаний 1 813; прод-`users` (migration, admin) сохранены. Орфанов 0, последовательности пересчитаны. Бэкапы в `backend/prod_backup_*.sql` и `backend/prod_users_backup_*.sql`.

---

## Технический долг (из первоначального анализа)

Первоначальный техдолг (1.1–1.10) **закрыт** — детали в «Статусе уже сделанного» внизу. Открытые долги:

| ID | Задача | Статус / детали |
|---|--------|-------------------|
| ТД-2 | ~~**Перед следующим разворачиванием на прод: пересоздать БД с ID=1 и чистой нумерацией**~~ (бывш. Б14) | ⏸ Отложено по решению владельца (09.2026): **напомнить перед разворачиванием на финальном прод-сервере** — сейчас работаем на пред-прод-сервере. На dev порядок отработан сценариями `backend/migrations/reimport/*` (перенумерация по хронологии, `recreate_and_resync.py`). Контроль: ID всех таблиц с 1, орфанов 0, последовательности пересчитаны. |
| ТД-3 | **Изоляция pytest от «живой» БД** | 🔨 Для CI закрыто (09.2026, вместе с п. 3.3): в `ci.yml` job `backend` гоняет тесты на **disposable-БД** `postgres:16` (alembic → init_data → pytest), живая БД не затрагивается. Локально тесты по-прежнему идут в реальный dev-PostgreSQL (маркеры + автоочистка после прогона). Тест сверки с файлом-источником (`test_control_sums_vs_source.py`) на пустой/свежей БД теперь пропускается (добавлен autouse-skip). При желании остаётся отдельная изолированная тестовая схема и для локального прогона. |

---

## UX и функциональность (раздел 2)

| ID | Задача | Статус / детали |
|---|--------|-------------------|
| 2.14 | **Разделить документ «Приходы/Расходы» на 4 документа: ПКО, РКО, платёжное поручение, поступление на счёт** | 🆕 Заявка владельца (09.2026). Сейчас один документ (`transactions` «Приход/Расход») с типом операции «приход/расход»; наличные и безнал не различаются. Цель: отдельные виды документов — «Приходный кассовый ордер» (приход в кассу), «Расходный кассовый ордер» (расход из кассы), «Платёжное поручение» (исходящий безнал), «Поступление на счёт» (входящий безнал): у каждого свой журнал/форма, авто-название и нумерация. **Пункты меню показываются динамически по типу кэшпоинтов (см. 2.15):** если есть кэшпоинт «Касса» → в меню «Приходный кассовый ордер» и «Расходный кассовый ордер»; если есть кэшпоинт «Счёт»/«Банк» → «Платёжное поручение» и «Поступление на счёт». Затрагивает модель/миграцию (тип документа), авто-названия (`services.build_transaction_title`), регистры, меню (`menu.ts`/`menuAccess.ts`), формы, отчёты. Решения владельца (06.09.2026): **нумерация — по видам** (у каждого документа своя серия/счётчик); **исторические «Приход/Расход» при миграции разделить на ПКО и РКО** — банковских счетов не было: приходы → ПКО, расходы → РКО, номера пересчитать по видам. Безнал — **вариант А**: единый «Регистр денежных средств», наличные и безнал в одной таблице; операция привязана к кэшпоинту, тип которого (Касса/Счёт, см. 2.15) определяет раздел/группировку в регистре и отчёте, остатки считаются отдельно по кассам и по счетам (+ общий итог). «Поступление на счёт» от жителя гасит долг во «Взаиморасчётах» (`accounts_register`) так же, как приход в кассу. **Уточнено (08.09): архитектурное решение в задаче 2.20 (отдельные типизированные документы касса/банк, статусы и сторно).** |
| 2.15 | **«Кассы/Счета» (`cash_points`): поле «Тип» = «Касса» или «Счёт»** | 🆕 Заявка владельца (09.2026, формулировка уточнена). Добавить в справочник поле «Тип» со значениями **«Касса»** и **«Счёт»** (банковский; владелец также называет его «Банк» — зафиксировать единое имя в UI). От типа зависит меню документов (см. 2.14): есть кэшпоинт «Касса» → показывать в меню «Приходный кассовый ордер» и «Расходный кассовый ордер»; есть кэшпоинт «Счёт» → показывать «Платёжное поручение» и «Поступление на счёт». Влияет на формы документов 2.14 и «Регистр денежных средств». **Предпосылка задачи 2.20 (тип кэшпоинта определяет меню/регистры).** |
| 2.18 | **Разовые и регулярные тарифы: не должно быть двух тарифов по одному виду услуги за один месяц** | 🆕 Заявка владельца (09.2026). Сейчас по одному виду услуги за месяц могут действовать и регулярный, и разовый тариф, из-за чего начисление считается неверно: показывается регулярный тариф, а разовый уходит в долг. Нужно решить вопрос учёта (ограничение «один тариф на вид услуги в месяц» либо корректная совместная обработка регулярного + разового при начислении и в долге). Затрагивает: тарифы/начисления (`services.calculate_accrual_*`), форму «Тарифы» (контроль пересечения периодов/признака разовости), пересчёт долга. Связано: Б11 (статусы тарифов «Действующий/Архивный»). |
| 2.19 | **Дашборд на главную страницу** | 🆕 Заявка владельца (09.2026). Главная после входа должна показывать сводные метрики: **Начислено, Внесено в кассу, Списано, Долг, Остаток в кассе**; а также блоки **«Должники» + динамика задолженности за последние 12 месяцев** и **«Расходы по кассе за текущий месяц»**. Источники уже готовы (регистры кассы, `reports/debtors`, `reports/cash_register`, `accounts_register`) — реализация: страница-дашборд + пункт меню для бухгалтерских ролей с переиспользованием существующих вычислений; динамика долга — агрегация по месяцам за 12 мес. |
| 2.20 | **Рефакторинг слоя денежных документов → «документ → регистр» (касса/банк, статусы и аудит)** | 🆕 Проектируется (08.09.2026). Архитектурный вариант А: отдельные типизированные документы «КассовыеДокументы» и «БанковскиеДокументы» (свои серии, статусы проведён/сторно, аудит вместо физического delete) поверх единого денежного регистра; журналы наличные/безналичные. Детальный план и фазы — `.ai/PLANS/2026-money-documents.md` (превосходит/развивает 2.14). |
| 2.21 | **Ссылочные поля: переход («проваливание») в запись, на которую ссылается поле, в отдельном окне** | 🆕 Заявка владельца (09.2026). В списках/формах с reference-полями (собственник, л/с, квартира, касса/счёт, статья, контрагент, вид услуги, счётчик, тариф, документ и т.п.) дать возможность из ссылочного значения «провалиться» в карточку/запись этой сущности. Предлагаемый UI — кнопка «…» в правой части поля (справа у значения или у reference-колонки/поля просмотра); по клику — открытие просмотра записи в отдельном окне/слое без потери текущего контекста. Открытые вопросы: где именно нужен переход (колонки списков и/или read-only поля форм просмотра), какой ресурс/идентификатор берётся из значения (нужно иметь `id`/`resource` у ссылки), права доступа при «проваливании». Связано: RecordFormModal/ReferenceSelect (фронт), сериализаторы со `*_id` и подставленными полями. |
| 2.2 | **Экспорт регистров и документов в Excel/CSV** | Кнопки «Экспорт» в списках; выгрузка с учётом текущих фильтров/пагинации. |
| 2.6 | **Настраиваемый доступ к формам/меню (админ)** | Сейчас права ролей зашиты в коде (`permissions.py`, `can.ts`, `menuAccess.ts`). Сделать форму настроек прав для администратора: доступ к разделам меню, формам и действиям (создание/изменение/удаление/чтение) по каждой роли. Хранение — таблица `role_permissions`, чтение в рантайме вместо/в дополнение к зашитым правилам. |
| Б2 | **ЛК жителя: редактирование контактных данных** | Телефон, почта и другие контактные данные собственника — кроме ФИО и логина; с ограничениями по правам. |
| Б3 | **Префикс л/с — настраиваемый админом** | Через форму «Настройки» (сейчас префикс генерации `account_number` — «LS/NNNN» — зашит). |

---

## Качество и надёжность (раздел 3)

| ID | Задача | Статус / детали |
|---|--------|-------------------|
| 3.2 | **Тесты фронтенда (vitest + RTL)** | 🔨 Частично (08.09). Ключевые сценарии: GenericList (рендер, сортировка, модалки) — впереди. Сделано: vitest/RTL добавлены в devDependencies + скрипт `test` (`vitest.config.ts`, jsdom-окружение, `src/test/setup.ts`); реализовано и покрыто тестами правило форматирования колонок (новый `config/columnAlign.ts`, зашит в `GenericList`): заголовки по центру, денежные → справа (2 знака, без `₸`), текстовые → слева, остальные → по центру; RTL-тесты на реальном списке «Приход/Расход» (`src/pages/GenericList.test.tsx`) + юнит-тесты `src/config/columnAlign.test.ts`. Просмотр записи уже: фон `#f5f5f5`/шрифт `#1f1f1f` у полей (`.form-view-mode`), даты `DD.MM.YYYY` (`RecordFormModal`) — осталось покрыть тестами. Продолжение (08.09, вне автотестов): правки интерфейса по «Доработкам» — реализованы п.1–6 (см. журнал PROJECT_STRUCTURE): общий просмотр без редактирования; reference-лейбл вместо id; единая компоновка Users; форматы дат `DD.MM.YYYY` в отчётах по кассе/расходам; срез должников на дату `as_of`; PDF по нажатию скачивается + `Content-Length` (надёжно на iOS). |
| 3.3 | **CI: автопроверка + подготовка релиза / выкат на прод** | 🔨 Каркас настроен (09.2026). В репозитории: `ci.yml` (push/PR в main: backend — disposable `postgres:16` → `alembic upgrade head` → `init_data.py` → `python -m pytest tests/ -q`, frontend — `npm ci` → `npm run build`); `docker-build.yml` (образы `backend/Dockerfile`+`frontend/Dockerfile` в ghcr.io: main → тег `main`, тег `v*` → semver); `deploy.yml` + `.github/actions/deploy` (выкат через `scripts/deploy_vps.sh <ref>`: по релизному тегу `v*` → прод, вручную → staging/production; деплой только при зелёном CI для коммита). `deploy_vps.sh` теперь принимает ref и пересобирает фронтенд. Осталось настроить вне кода: SSH-секреты, окружения `staging`/`production`, required checks CI на `main`, при необходимости required reviewers на `production` (пошагово — DEPLOY.md §10). Тестовый сервер для сверки данных: развернуть по DEPLOY.md §3 и выкатывать окружением `staging`. |
| 3.4 | **Полный аудит названий документов и всех л/с** | ⏳ Частично: чистка «мусорных» квартир/контрагентов и орфанов выполнена (Б12), `check_register_integrity`/rebuild есть и покрыты тестами. Полный аудит названий документов/всех л/с ещё не проводился. |

---

## Бэклог доработок

Заявки оператора/заказчика. Не закрыты — по мере приоритизации.

| ID | Задача | Статус / детали |
|---|--------|-------------------|
| Б8 | **Сведения о жильцах в «Квартирах» → тариф «По жильцам»** | Две связанные части: (1) **сведения о жильцах** в Справочнике «Квартиры» (состав жителей по квартире: кто проживает, с какого периода и т.п.); (2) после этого — **расчёт тарифа «По жильцам»** от числа жильцов (тип в `tariff_types` + поддержка в `calculate_accrual_for_account_service`). Ранее отложено до отдельного решения — актуализировать требования (какие именно данные о жильцах нужны). Долгосрочно пересекается с идеями «Регистры сведений»/«Регистрация жителей» (И1/И2). |
| Б9 | **ЛК: принудительная смена пароля при первом входе** | ⏳ Частично (09.2026): жителям заведены логины `fth001…fth017` с привязкой к своему ЛС (пароль по умолчанию `fth123`, `full_name` — ФИО собственника). Осталось: флаг «сменить пароль при первом входе» + форма смены пароля. |

---

## Приоритеты (актуальный порядок работ)

> **Обновлено 08.09.2026:** предыдущий список (ТД-1, 2.7, 3.3, 2.3+Б15) полностью
> закрыт/зачёркнут — см. «Ранее закрыто» ниже. Ниже — **открытые** задачи по степени готовности
> к ним (зависимости) и приоритету владельца.

1. **Ввести CI/CD в строй (3.3, «Что осталось сделать»)** — инфраструктурный гейт перед новым продом:
   выбрать серверы staging/production и способ доставки, добавить SSH-секреты (`SSH_HOST`/`SSH_USER`/`APP_DIR`)
   и публичный ключ на серверах, при необходимости — required reviewers, поднять тестовый сервер и сделать
   первый деплой из Actions (пошагово — `DEPLOY.md §10`).
2. **2.14 + 2.15 — разделение «Кассы/Счета» на Касса/Счёт и документы ПКО/РКО/платёжное поручение/поступление на счёт**
   (две связанные заявки владельца). Идти вместе: сначала тип кэшпоинта (2.15), затем четыре вида документов (2.14)
   с нумерацией по видам и разделением истории на ПКО/РКО (вариант А — единый регистр ден. средств).
3. **2.18 — корректный учёт регулярного + разового тарифа по виду услуги за месяц** — базовая корректность начислений
   (базируется на уже готовых статусах тарифов Б11).
4. **2.19 — дашборд на главную** — переиспользует готовые источники (`reports/debtors`, касса, регистры).
5. **2.2 — экспорт регистров и документов в Excel/CSV** (с учётом текущих фильтров/пагинации).
6. **3.2 — тесты фронтенда (vitest + RTL)** — начало: GenericList (рендер/сортировка/модалки).
7. **2.6 — настраиваемый админом доступ к формам/меню** (`role_permissions` + форма настроек).
8. **Бэклог и качество по мере необходимости:** Б9 (смена пароля при первом входе) → Б2/Б3 (ЛК-контакты/префикс л/с),
   Б8 (жильцы + тариф «По жильцам»), 3.4 (полный аудит названий документов и всех л/с),
   ТД-3 (опционально: изоляция локальных тестов от «живой» dev-БД),
   ТД-2 (напомнить **перед финальным прод-развёртыванием**: пересоздать БД с ID=1 и чистой нумерацией).

**Ранее закрыто** (детали и код — в «Статус уже сделанного» ниже): технический долг 1.1–1.10, списание 2.5/2.8,
отчёты 2.11, ЛК жителя + роль auditor, миграция истории 4.1–4.4, ZIP квитанций (2.4), колонки/порядок (2.10-A/C),
ресайз (2.1), настройки пользователя (2.13), компоновка (2.12), кнопки-иконки (Б7), показания/«Тариф» (Б4+Б13),
статус тарифов (Б11), Б10 (фильтрация), ТД-1 (PDF под авторизацией), 2.7, 2.16, 2.17, 2.3+Б15, примечание квитанций (Б6),
чистка «Видов услуг», прод-накат (сен. 2026).

---

## Идеи развития (пока не проработанные детально, как идеи)

| ID | Идея | Суть / почему это отдельная тема |
|---|--------|------------------------------------|
| И1 | **«Регистры сведений»** | Хранить данные на конкретный момент времени: «Собственники», «Тарифы», «Сведения о квартирах» и т.п. Сейчас история есть частично (тарифы — `tariffs.valid_from`), по собственникам/составу квартир истории нет. Архитектурно крупная переделка модели данных — прорабатывать отдельно. |
| И2 | **Документы для изменения регистров сведений** | «Регистрация собственника», «Регистрация счётчика», «Регистрация жителей» и т.п. (1С-подобный документооборот поверх И1). Логическое продолжение И1. |
| И3 | **Расширения ЛК (по желанию)** | Онлайн-оплата, подача показаний из ЛК, история начислений/платежей (последнее частично покроет задача 2.3+Б15). |
| И4 | **Экспорт в Excel + брендинг управленческих отчётов** | PDF-часть уже закрыта задачей 2.16 (касса/расходы/должники/выписка — PDF). Осталось: выгрузка в **Excel** и **брендинг** (логотип/реквизиты/шаблон) отчётов, а также единый механизм «сформировать → скачать». |

---

## Статус уже сделанного (справочно)

- ✅ **П. 1.4 Alembic — схема под контролем**: БД на head (`0015_user_preferences`), исторические ручные SQL-миграции инкорпорированы в `0002_schema_squash`. `alembic revision --autogenerate` не выявляет ручных правок сверх моделей; весь «дрейф» сводится к типу `users.role` (VARCHAR↔Enum, косметический).
- ✅ **П. 1.2 — удалён мёртвый `schemas.py`**: Pydantic-модели не использовались и устарели (нет новых сущностей/полей), API работает на `dict` через `FIELD_CONFIG`/сериализаторы. Удалено.
- ✅ **П. 1.1 — распил `app.py`**: `app.py` с 3035 строк сокращён до ~680. Вынесены `field_config.py`, `serializers.py`, `services.py`, `routers/{documents,registers,receipts,others}.py`. `app.py` содержит только auth, `/meta` и generic CRUD. pytest 31/31, полный smoke по всем группам эндпоинтов — OK.
- ✅ **П. 2.5/2.8 — списание отдельным документом**: модели `writeoff_documents`/`writeoff_items` + `writeoff_id` в `accounts_register`; `create_writeoff_document`/`cancel_writeoff_document`; API `run`/`cancel`/`items`; меню и журнал «Списания» с просмотром и отменой; кнопка «Выполнить списание» перенесена в журнал; автозапуск при «Приход/Расход» — через документ.
- ✅ Целостность `accounts_register`: пересчёт балансов «с нуля» по истории.
- ✅ **Пересчёт регистров с нуля + идемпотентность**: обобщён `recalculate_register_balance` (для `accounts_register`/`cash_register`); добавлены `rebuild_accounts_register` (полная пересборка производного среза из `accruals_register`+`cash_register`) и `check_register_integrity` (аудит согласованности) + эндпоинт `POST /api/maintenance/rebuild_registers`. pytest покрывает восстановление испорченного среза и детерминизм повторного пересбора.
- ✅ **Формальный «переворот» знаков**: начисления в `income`, списание/оплата в `expense`; `balance_after>0`=долг, `<0`=переплата. Квитанции и отчёт считают долг/переплату из регистров (`_account_debt_overpayment`), переплата видна в отчёте (`overpayment`) и в `cash_register`. Скрипт миграции истории `migrations/flip_accounts_register_signs.py`.
- ✅ «Вид услуги» в регистре взаиморасчётов + сортировка по «№ квартиры» и «Документ».
- ✅ «№ квартиры» в двух регистрах; корректная серверная сортировка по квартире.
- ✅ Поля «Дата» и «Название» в документе «Приход/Расход» с авто-пересчётом.
- ✅ Убран столбец «Действия» в регистрах; единый порядок колонок в похожих формах.
- ✅ Тесты формул начислений по типу тарифа (Фиксированный / По площади / По счетчику) — `test_accrual_formulas.py`.
- ✅ **UI справочников (`ReferenceSelect`)**: показываются все записи (не только первые 10 — обход Refine-пагинации через `authedFetch`); для необязательных полей (квартира) в списке есть пункт «— пусто —», чтобы очистить значение выбором из списка (операция снова может быть «без квартиры»), а не только крестиком.
- ✅ **Выписка/сводка по л/с**: `GET /api/accounts/{id}/statement` и `GET /api/me/statement` (общий `build_account_statement`): начислено/внесено/списано/долг/остаток + детализация по услугам.
- ✅ **Отчёты**: «По кассе» (`/api/reports/cash_register`), «По расходам» (`/api/reports/expenses`), «Выписка по счёту» (`/api/reports/statement`, помесячно), «По должникам» (`/api/reports/debtors`) + страницы `CashReport`/`ExpenseReport`/`StatementReport`/`DebtorsReport`.
- ✅ **Авто-названия документов (п. 1.9) и запрет будущих дат (п. 1.10)**: «Приход/Расход» — «Приход в кассу №…», начисления — «Начисление за <месяц>», показания — «Показания за <месяц> — <услуга>», списания — «Списание задолженностей №…»; `validate_date_not_future` на дату операции/показаний/начислений/период квитанций.
- ✅ **Ресайз ширины колонок (2.1, 09.2026)**: ручка на правом краю заголовка (pointer-события, ширина = стартовая + дельта указателя — работает при любом scroll, включая `max-content`, без библиотеки); ширины — в localStorage по (ресурс, роль) вместе с порядком/видимостью (`useColumnSettings` → `{order, hidden, widths}`); ручка не конфликтует с drag&drop заголовков и сортировкой.
- ✅ **Порядок колонок списков (2.10-C, 09.2026)**: перестановка drag&drop самих заголовков таблицы и в панели «Отображаемые колонки» (`@dnd-kit/*`, `SortableColumns.tsx`/`ColumnDragTitle`); хранение `{order, hidden}` в localStorage по (ресурс, роль) — `hooks/useColumnSettings.ts` (старый формат видимости мигрируется; новые колонки конфига видимы и дописываются в конец); «ID»/«Действия» закреплены. Ранее не работавший HTML5-DnD код удалён вместе с `useVisibleColumns.ts`.
- ✅ **Статус тарифа «Действующий/Архивный» (Б11, 09.2026)**: колонка `tariffs.status` (миграция `0014_tariff_status`, бэкафилл по группам «вид услуги + разовость»); при создании нового тарифа предыдущие действующие той же группы архивируются (`services.retire_tariff_predecessors`); UI: колонка «Статус» + фильтр по статусу, по умолчанию «Действующие».
- ✅ **«Тип тарифа» и «Ед. изм.» — на виде услуги (06.09.2026)**:одна привязка на услугу (`services_type.tariff_type_id` NOT NULL FK, `services_type.unit`), наследуется всеми тарифами. Миграции `0012_service_tariff_type`/`0013_service_unit` (бэкафилл: регулярный тариф → любой тариф → «Фиксированный»/пусто). Расчёт (`services.calculate_accrual_*`) и метры (`service_supports_meter`) — по типу услуги; `has_meter_tariff` = тип «По счетчику»; формы/колонки/сортировка обновлены; начисление 08.2026 не изменилось (контроль до/после); pytest 42 passed.
- ✅ **Развёртывание ПК: HTTP-only + зеркальная синхронизация (06.09.2026)**: nginx наружу только :80 (HTTPS-сертификат удалён, HTTPS — опционально через NAS); симметричный режим corsus ↔ snowflake (дампы через Synology Drive): автодамп перед push (`pre-push`), автоимпорт/догон схемы после pull (`post-merge`), DEPLOY §7.2–7.5.
- ✅ **2.4 — ZIP всех квитанций периода (09.2026)**: `POST /api/receipt_documents/bulk_pdf` (ZIP-архив) + кнопка «Скачать (ZIP)» в `ReceiptsModal.tsx`. Осталось (по желанию, не блокирует): брендинг/шаблон квитанции (логотип, реквизиты) — см. И4/2.16.
- ✅ **2.13 — серверные настройки пользователя (09.2026)**: таблица `user_preferences` (user_id, resource, data JSON; миграция `0015_user_preferences`) + `GET/PUT /api/preferences[/{resource}]` (`backend/routers/preferences.py`); фронт: `frontend/src/auth/preferences.ts` (сервер + локальный кэш по username), `hooks/useColumnSettings.ts` переработан в чистую логику (настройки приходят снаружи, хранилище — в GenericList), `GenericList` сохраняет/восстанавливает сортировку, применённые фильтры (и панель «Фильтры»), видимость/порядок/ширины колонок и pageSize; изменения уходят на сервер с debounce; настройки переживают перезагрузку и смену устройства.
- ✅ **2.12 — единая компоновка списков (09.2026)**: убрана колонка «Действия» из строк; в списке выделяется строка (radio/кликом), под заголовком слева появляется панель действий записи («Редактировать», «Удалить» с подтверждением; спец.: «Просмотр»/«Отменить» у списаний, «Просмотр»/«PDF»/«Удалить» у квитанций, «Редактировать»/«Удалить» у документов начислений/показаний) — по правам роли. Справа над таблицей — панель действий таблицы: сортировка (селект колонки + направление), «Фильтры», «Колонки», «Добавить» и спец-действия. В регистрах (только чтение) выбора нет.
- ✅ **Б7 — кнопки-иконки с tooltip (09.2026)**: в панелях списков текстовые кнопки заменены на иконки со всплывающими подсказками (Фильтры с бейджем-счётчиком, Колонки, Добавить/начислить/сформировать квитанции/списание, действия записи: просмотр/редактирование/PDF/удаление/отмена). В модальных окнах стандартные текстовые кнопки сохранены (ОК/Отмена/Сохранить — анти-паттерн менять).
- ✅ **2.7 — счётчик только к услугам «По счетчику» (09.2026)**: тип тарифа закреплён на виде услуги (`services_type.tariff_type_id`); бэкенд `service_supports_meter`/`validate_meter_service_type` — 422 при попытке завести/перевести счётчик на услугу без типа «По счетчику»; фронт: признак `has_meter_tariff` (= тип услуги «По счетчику») фильтрует «Вид услуги» в форме «Счетчики».
- ✅ **Б4+Б13 — показания и «Тариф» в «Регистре начислений» (09.2026)**: исторические строки регистра наполнены показаниями — `backend/migrations/fill_accrual_readings.py`: для месяца ищет пару «текущее/прошлое» по показаниям счётчиков квартиры (с учётом замены приборов M-XX-1→M-XX-2; пара, где разность = consumption); заполнено 1745/1748 (3 — на стыке замены без предыдущего показания), consumption не менялся; при генерации начислений фиксируется `current_reading_id`. Фронт: колонка «Тариф» (`tariff.price` + наследуемая `unit`). Примечание по данным: показания есть только у услуг «По счетчику» (Электроэнергия) — у фиксированных услуг прочерки корректны; регистр открывается «Период» ↓.
- ✅ **2.16 — экспорт отчётов в PDF (09.2026)**: PDF для «По кассе», «По расходам», «По должникам» — `backend/statement_pdf.build_*_report_pdf`, эндпоинты `/reports/<...>/pdf` (выписка по счёту и сводка ЛК уже были); кнопки «PDF» на страницах отчётов через `openAuthorizedPdf`.
- ✅ **2.17 — сортировка столбца: только 2 состояния (09.2026)**: на сортируемые колонки `GenericList` добавлен `sortDirections=['ascend','descend']` — повторный клик переключает возр./убыв., третьего состояния (сброс) нет.
- ✅ **2.3 + Б15 — движения денег по лицевому счёту + PDF выписки + период (09.2026)**: блок «Движения по счёту» в ЛК жителя и просмотре администратора (`GET /api/me/movements`, `/api/accounts/{id}/movements`, с `from_date`/`to_date`); RangePicker в ЛК и в отчёте «Выписка по счёту»; PDF выписки (`backend/statement_pdf.py`) — детализация (`/me/statement/pdf`, `/accounts/{id}/statement/pdf`) и помесячная (`/reports/statement/pdf`); кнопки PDF через `openAuthorizedPdf`.
- ✅ **Б6 — «Квитанции»: поле «Примечание» (09.2026)**: `receipt_documents.comment` (String 500, миграция `0016`), сериализатор + колонка «Примечание»; безопасный `PATCH /receipt_documents/{id}/comment` (правит только примечание, суммы не трогаются); в `ReceiptViewModal` — поле с кнопкой «Сохранить».
- ✅ **И5 — «Расходы по кассе» в ЛК жителя (09.2026)**: блок «Расходы по кассе» (`CabinetView.tsx`, RangePicker периода + таблица по статьям + «Итого расходов») показывается в ЛК жителя (`ResidentCabinet`) и в админ-просмотре счёта (`AdminCabinet`, `houseExpenses=true`). Источник — `GET /api/me/house_expenses` (`routers/others.py`): разбивка по статьям и общая сумма из того же источника, что «Отчёт по расходам» (`cash_register`); персональные операции/контрагенты жителю не раскрываются.

### ✅ Операция «Списание задолженностей» (Фазы 0–3)
- ✅ Конвенция знаков зафиксирована комментариями в `models.py`/`app.py` (блок «КОНВЕНЦИЯ ЗНАКОВ»).
- ✅ Регистр денежных средств `cash_register` (миграция `0001`): «Приход/Расход» пишет сюда, а не во взаиморасчёты.
- ✅ `services_type.priority` (миграция `0002`) — приоритет списания услуги.
- ✅ Ядро `backend/writeoffs.py` `calculate_write_offs` — распределение денег по приоритету, идемпотентно, переплата висит отрицательным остатком (вариант 1).
- ✅ **Документный слой списания (п. 2.5/2.8)**: `create_writeoff_document`/`cancel_writeoff_document`, журнал «Списания», кнопка «Выполнить списание» в журнале (вместо устаревшей страницы «Приход/Расход»); `write_offs/run` заменён на `writeoff_documents/run`.
- ✅ Кнопка «Выполнить списание» (журнал «Списания», `WriteOffsModal`).
- ✅ Автозапуск списания при создании документа «Приход/Расход» (через документ).
- ✅ Отчёт по лицевому счёту `GET /api/accounts/{id}/statement`: начислено / оплачено / долг по услугам, внесено / переплата (для отчёта и будущего ЛК).
- ✅ pytest: тесты в `backend/tests/test_writeoffs.py` (регистр денежных средств, приоритет списания, идемпотентность, переплата, отчёт, пересборка среза) — зафиксирована логика Фаз 1–4.
- ✅ Скрипт миграции исторических данных `backend/migrations/migrate_old_payments_to_cash_register.py` (перенос старых оплат из `accounts_register` в `cash_register` с защитой от дублей).

### ✅ Аутентификация и ролевой доступ
- ✅ Пункт 1.3: модель `User` + роль (6 ролей: admin/operator/cashier/controller/resident/auditor), миграция Alembic `0001_users` (legacy `users` пустая — пересоздана чисто). Роль `auditor` добавлена позже (09.2026) — миграция БД не требуется (`users.role` — VARCHAR).
- ✅ JWT-логин: `POST /api/auth/login`, `GET /api/auth/me`, `POST /api/auth/users` (только admin). Хеш паролей PBKDF2-SHA256 (stdlib).
- ✅ Ролевой доступ: `permissions.py` (generic CRUD по (метод, ресурс)) + защита спец-эндпоинтов (начисление/списание/пересбор/отчёт).
- ✅ CORS: явные origins из `CORS_ORIGINS` (по умолчанию — `http://localhost:5173` для разработки; прод-домен задаётся в `.env`), вместо `['*']`.
- ✅ CLI: `backend/create_user.py` для первоначального заведения админа.
- ✅ pytest: тесты auth (хеш, JWT, login, все роли) + HTTP-тесты ролевого доступа через TestClient (httpx).
- ✅ Роль `auditor` («Аудитор», 09.2026): read-only — просмотр всех разделов/регистров/отчётов и ЛК/выписки любого счёта; изменения запрещены на всех эндпоинтах (generic CRUD — `permissions._read_allowed`; кастомные write-эндпоинты документов/квитанций — `require_write_access`; настройки/регистры-записи недоступны). Фронт: меню показывает все разделы (`menuAccess.ts`), кнопки добавления/изменения/удаления скрыты (`can.ts`); «Пользователи и права» остаётся только у admin.
- ✅ Фиксы входа (09.2026): после логина пользователь сразу попадает в свой раздел — роль/ресурсы пересчитываются по событию авторизации `townhouse:auth-change` (житель больше не «падал» в недоступный «Приход/Расход» с 403); форма входа защищена от двойной отправки.
- ✅ **Личный кабинет жителя (ЛК)**: привязка `User.account_id` (миграция `0003_resident_account_link`), эндпоинты `GET /api/me/statement` (сводка по своему счёту) и `GET /api/me/receipts` (свои квитанции), ограничение `resident` только своим счётом/квитанциями (`_ensure_can_view_account`, `_raise_for_resident_other` для statement/items/pdf). Фронт: страница `ResidentCabinet.tsx` (сводка + детализация по услугам + список квитанций с просмотром и PDF), меню «Мой кабинет» — только роль `resident`. Seed привязывает `resident` к `FTH-001`.
- ✅ **ТД-1 — PDF квитанций под авторизацией (09.2026)**: кнопки «PDF» открывали `/receipt_documents/{id}/pdf` через `window.open` без Bearer-токена → 401 «Требуется авторизация». Добавлен `openAuthorizedPdf` (`frontend/src/auth/http.ts`): авторизованный fetch → blob-URL в новой вкладке (при блокировке попапа — скачивание). Заменены вызовы в `CabinetView.tsx` (ЛК), `ReceiptViewModal.tsx` (просмотр) и `GenericList.tsx` (журнал квитанций).
- ✅ **Чистка «Видов услуг» (09.2026)**: дубль «Электричество» удалён (`migrations/dedupe_electricity_service.py`; каноническая — «Электроэнергия»); услуга «Прочие расходы» удалена — её начисления переведены на «Фонд развития» с разовыми тарифами 6000/7560 ₸ (`move_prochie_rashody_to_fond.py` + `fix_fond_oneoff_tariffs_for_prochie.py`; пересборка `accounts_register`, перегенерация квитанций; контроль: начисления 10 179 / 60 008 492,00; квитанции 1 816 / 60 008 492,00). В `init_data.py` из дефолтов убраны «Электричество»/«Холодная вода».
- ✅ **Контрольные суммы регистров vs файл-источник**: `tests/test_control_sums_vs_source.py` сверяет начисления (по квартирам/итог/кол-во) и кассу (расход; приход по квартире нетто сторно; приход итог + входящее сальдо) с `templates/Миграция данных FTH.xlsx`; `openpyxl` добавлен в `requirements.txt`; без файла/пакета тесты пропускаются (на пустой БД CI — тоже skip).
- ✅ **Б10 — общий механизм фильтрации списков (09.2026)**: серверный фильтр `backend/filtering.py` (формат `<field>[_{like|ne|gte|lte}]=<value>`, разрешение полей как у сортировки — прямые столбцы, вложенные по relationship, агрегаты, автор; автотипизация bool/числа/даты/datetime; contains — ILIKE; eq/ne для нативных PG-enum по имени члена) + кнопка «Фильтры» в `GenericList` с контролами по типу колонки (текст/диапазон от-до/период/Да-Нет/выбор) и счётчиком активных фильтров; тесты `test_filtering.py`; фронт-конфиг `frontend/src/config/filters.ts`. Доработки (07.09): исправлен 500 на фильтрах по вложенным полям-справочникам (`_kind_of_type` вместо повторного `.type` — contains/eq по `owner.full_name` и т.п. заработали); колонки-справочники фильтруются **select со значениями из того же справочника** (`ReferenceFilterSelect`, кэш по ресурсу); ресурсо-зависимые select-поля («Статус» у тарифов); **стартовые фильтры раздела** («Тарифы» по умолчанию — «Действующие»); «Регистр начислений» открывается по «Периоду» ↓. pytest 47 passed.
