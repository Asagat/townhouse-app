# Roadmap — Townhouse ERP

Сводный план развития проекта. Дата составления: август 2026.
Все изменения фиксируются в git (ветка `main`), каждое направление — отдельными коммитами.

---

## 1. Технический долг (из первоначального анализа)

| # | Задача | Обоснование / как |
|---|--------|-------------------|
| 1.1 | Разбить `backend/app.py` (~1700 строк) на модули | Единый файл содержит сериализаторы, конфиг полей (`FIELD_CONFIG`), билдеры и все роутеры. Разделить: `serializers.py`, `field_config.py`, `routers/{crud,registers,documents}.py`. Снижает риск регрессий и упрощает тесты. |
| 1.2 | Убрать или оживить `schemas.py` | Pydantic-модели сейчас не используются (API работает на голых `dict`) и разошлись с реальностью. Либо удалить, либо перевести ответы на эти модели. |
| 1.3 | Аутентификация и закрыть CORS | ✅ Реализовано: модель `User` + роли, JWT-логин (`/api/auth/login`, `/api/auth/me`, `/api/auth/users`), PBKDF2-хеши, CORS через `CORS_ORIGINS` вместо `['*']`. См. секцию «Аутентификация и ролевой доступ». |
| 1.4 | Ввести миграции БД (Alembic) | ⏳ Частично: Alembic подключён (ревизии `0000_baseline`, `0001_users`), но большая часть схемы пока правится вручную `ALTER TABLE`. Перевести остальные изменения на миграции. |
| 1.5 | Серверная сортировка по вложенным полям | `hasattr(model, "owner.full_name")` всегда `False` → сервер молча сортирует по `id`. Для вложенных колонок добавить подзапросы (как уже сделано для квартиры и документа). |
| 1.6 | Почистить «осиротевшие» строки в БД | В `accounts_register` есть строки без привязки к `transaction_id`/`accrual_id` (артефакты до рефакторинга). Решить: удалить или привязать. |
| 1.7 | Проверить пересчёт балансов при массовых правках | Текущий пересчёт «с нуля» корректен, но добавить тесты и, при необходимости, репересчёт всех аккаунтов служебной командой. |
| 1.8 | Ни один документ не хранит автора/историю изменений | Выявлено при анализе: `Transaction`, `AccrualDocument`, `MeterReadingDocument`, `ReceiptDocument` не фиксируют `created_by`/`updated_by`/`updated_at`/`change_description`. Функциональная реализация — см. п. 2.9; здесь это учтено как технический долг (нужна целостная аудит-модель для всех документов). |

---

## 2. UX и функциональность

| # | Задача | Обоснование / как |
|---|--------|-------------------|
| 2.1 | Ресайз и перестановка колонок через библиотеку | Ручная реализация (HTML5 DnD) в AntD Table не заработала при `scroll={{x:'max-content'}}` и была удалена. Надёжно делается через `react-resizable` (ресайз) и `@dnd-kit` / `react-dnd` (перестановка). Требует добавления npm-зависимости. |
| 2.2 | Экспорт регистров и документов в Excel/CSV | Кнопки «Экспорт» в списках; выгрузка с учётом текущих фильтров/пагинации. |
| 2.3 | Отчёты по лицевым счетам | Начислено / оплачено / долг за период, сводка по дому. |
| 2.4 | Квитанции: создание, PDF, сохранение | ✅ Реализовано: модели `receipt_documents`/`receipt_items`, генерация по всем счетам за период, долг/переплата на «Фонд развития», PDF через `reportlab` (столбец «Переплата»), реализуем в UI (меню + ReceiptsModal). Осталось: выгрузка ZIP всех квитанций периода, при необходимости брендинг. |
| 2.5 | **Списание — отдельным документом** | Сейчас списание запускается сервисной операцией (`POST /api/write_offs/run`, кнопка/cron/автозапуск при «Приход/Расход») и пишет напрямую в регистры. Перевести на **документ «Списание задолженности»** с собственной моделью (`writeoff_documents`/`writeoff_items`), чтобы у операции были атрибуты документа (дата, №, автор, статус), возможность отмены/пересоздания и записи в журнале. Ядро `calculate_write_offs` при этом остаётся основой, но оборачивается в документооборот. |
| 2.6 | **Настраиваемый доступ к формам/меню (админ)** | Сейчас права ролей зашиты в коде (`permissions.py`, `can.ts`, `menuAccess.ts`). Сделать **форму настроек прав** для администратора, где можно включать/выключать доступ к разделам меню, формам и действиям (создание/изменение/удаление/чтение) по каждой роли. Хранение — таблица настроек доступа (например `role_permissions`), доступ к которой читается в рантайме вместо/в дополнение к зашитым правилам. |
| 2.7 | **Счетчик — только по тарифам «По счетчику»** | При создании счётчика (`meters`) разрешать выбор услуги только тех `services_type`, у которых тип тарифа «По счетчику» (иначе счётчик бессмыслен). Ограничение и на бэкенде (валидация при создании/изменении счётчика), и на фронте (фильтр выпадающего списка внешних ключей). |
| 2.8 | **Переделать обработчики «Выполнить списание»** | Переработать `WriteOffsModal` и обработчики запуска списания (кнопка + автозапуск при «Приход/Расход» + cron), чтобы они опирались на новый документный слой (см. п. 2.5), давали понятный результат/ошибки по каждому счёту и не создавали частичных записей при сбое. |
| 2.9 | **Аудит документов: автор и история изменений** | У каждого документа завести метаданные изменений: `created_by` (автор), `updated_by` (автор последнего изменения), `updated_at` (дата последнего изменения) и при желании `change_description` (что именно изменилось). Сейчас ни один документ не фиксирует автора (`Transaction`, `AccrualDocument`, `MeterReadingDocument`, `ReceiptDocument`). Реализация: миграции на модели + простановка текущего юзера при создании/правке (в generic `create_resource_item`/`update_resource_item` и спец-эндпоинтах) + вывод «Автор»/«Изменён»/«Что изменено» в формах и журнале. Согласуется с п. 2.5 (новый документ списания должен иметь автора с самого начала). |

---

## 3. Качество и надёжность

| # | Задача | Обоснование / как |
|---|--------|-------------------|
| 3.1 | Тесты бэкенда (pytest) | Покрыть расчёт начислений, пересчёт балансов `accounts_register`, CRUD. Защита от регрессий (напр., как с ресайзом, который нельзя было заметить без проверки в браузере). |
| 3.2 | Тесты фронтенда (vitest + RTL) | Ключевые сценарии GenericList: рендер, сортировка, модалки. |
| 3.3 | CI (при желании) | Прогон `tsc`, `build`, pytest и, опционально, lint при пуше. |
| 3.4 | Проверка и чистка накопленных данных | Аудит целостности `accounts_register` (балансы сходятся с историей), корректность названий документов. |

---

## Приоритеты (предлагаемый порядок)

1. **1.3 Аутентификация + CORS** — критично для продакшена.
2. **1.4 Alembic** — снимает ручное управление схемой.
3. **1.1 Распил `app.py`** — облегчает всё остальное и тесты.
4. **3.1 Тесты бэкенда** — фиксируют текущую логику начислений/балансов.
5. Остальное (UX 2.x, экспорт, отчёты, квитанции 2.4) — по мере необходимости.

---

## Статус уже сделанного (справочно)

- ✅ Целостность `accounts_register`: пересчёт балансов «с нуля» по истории.
- ✅ **Пункт 3.3 (пересчёт регистров с нуля + идемпотентность)**: обобщён `recalculate_register_balance` (для `accounts_register`/`cash_register`); добавлены `rebuild_accounts_register` (полная пересборка производного среза из `accruals_register`+`cash_register`) и `check_register_integrity` (аудит согласованности) + эндпоинт `POST /api/maintenance/rebuild_registers`. pytest покрывает восстановление испорченного среза и детерминизм повторного пересбора.
- ✅ **Формальный «переворот» знаков**: начисления в `income`, списание/оплата в `expense`; `balance_after>0`=долг, `<0`=переплата. Квитанции и отчёт считают долг/переплату из регистров (`_account_debt_overpayment`), переплата видна в отчёте (`overpayment`) и в `cash_register`. Скрипт миграции истории `migrations/flip_accounts_register_signs.py`.
- ✅ «Вид услуги» в регистре взаиморасчётов + сортировка по «№ квартиры» и «Документ».
- ✅ «№ квартиры» в двух регистрах; корректная серверная сортировка по квартире.
- ✅ Поля «Дата» и «Название» в документе «Приход/Расход» с авто-пересчётом.
- ✅ Убран столбец «Действия» в регистрах; единый порядок колонок в похожих формах.
- ❌ Ресайз/перестановка колонок — отменено (не работало; см. 2.1).

### ✅ Операция «Списание задолженностей» (Фазы 0–3)
- ✅ Конвенция знаков зафиксирована комментариями в `models.py`/`app.py` (блок «КОНВЕНЦИЯ ЗНАКОВ»).
- ✅ Регистр денежных средств `cash_register` (миграция `0001`): «Приход/Расход» пишет сюда, а не во взаиморасчёты.
- ✅ `services_type.priority` (миграция `0002`) — приоритет списания услуги.
- ✅ Ядро `backend/writeoffs.py` `calculate_write_offs` — распределение денег по приоритету, идемпотентно, переплата висит отрицательным остатком (вариант 1).
- ✅ Эндпоинт `POST /api/write_offs/run` (кнопка + cron).
- ✅ Кнопка «Выполнить списание» (страница «Приход/Расход», `WriteOffsModal`).
- ✅ Автозапуск списания при создании документа «Приход/Расход».
- ✅ Отчёт по лицевому счёту `GET /api/accounts/{id}/statement`: начислено / оплачено / долг по услугам, внесено / переплата (для отчёта и будущего ЛК).
- ✅ pytest: 8 тестов в `backend/tests/` (регистр денежных средств, приоритет списания, идемпотентность, переплата, отчёт) — зафиксирована логика Фаз 1–4.
- ✅ Скрипт миграции исторических данных `backend/migrations/migrate_old_payments_to_cash_register.py` (перенос старых оплат из `accounts_register` в `cash_register` с защитой от дублей).
- ⏳ **След. шаг (п. 2.5)**: вынести списание в отдельный документ `writeoff_documents`/`writeoff_items` (дата, №, автор, статус, отмена/пересоздание) вместо прямой сервисной операции.

### ✅ Аутентификация и ролевой доступ
- ✅ Пункт 1.3: модель `User` + роль (5 ролей: admin/operator/cashier/controller/resident), миграция Alembic `0001_users` (legacy `users` пустая — пересоздана чисто).
- ✅ JWT-логин: `POST /api/auth/login`, `GET /api/auth/me`, `POST /api/auth/users` (только admin). Хеш паролей PBKDF2-SHA256 (stdlib).
- ✅ Ролевой доступ: `permissions.py` (generic CRUD по (метод, ресурс)) + защита спец-эндпоинтов (начисление/списание/пересбор/отчёт).
- ✅ CORS: явные origins из `CORS_ORIGINS` (по умолчанию `townhouse.sagacloud.kz` + localhost), вместо `['*']`.
- ✅ CLI: `backend/create_user.py` для первоначального заведения админа.
- ✅ pytest: тесты auth (хеш, JWT, login, andери-роли) + HTTP-тесты ролевого доступа через TestClient (httpx).
- ⏳ Экран ЛК резидента (только свой счёт) — отдельный этап.
- ⏳ Дальнейшее: Alembic вместо ручных миграций, аутентификация, экран ЛК потребителя (использует `statement`).
