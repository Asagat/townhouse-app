# Roadmap — Townhouse ERP

Сводный план развития проекта. Дата составления: август 2026. Все изменения фиксируются в git (ветка `main`), каждое направление — отдельными коммитами.

> **Заметки (сентябрь 2026)**
> - Денежные сущности теперь несут **два независимых среза** для подробных отчётов:
>   - **«Статья»** (`transactions.article_id` → `analytic_articles`, статьи доходов/расходов) — тип движения денег; **«Контрагент»** (`transactions.contractor_id` → `counterparties`) — связанная сторона операции. Оба поля продублированы/доступны в «Регистре денежных средств» (контрагент — собственный столбец `cash_register.contractor_id`). Миграции Alembic: `0006_cash_service_link` и `0007_document_create_ts` применены к живой БД.
> - **«Дата документа» и «Дата создания»** разделены у всех документов. У «Приход/Расход» (`transactions`) и «Квитанций» (`receipt_documents`) заведена отдельная колонка `created_at` — миграция `0007_document_create_ts` применена к живой БД; существующие строки дохистории заполнены датой документа (`transaction_date`/`issued_at`). В UI у «Приход/Расход» и «Квитанций» выведены обе колонки («Дата документа» и «Дата создания»).
> - Справочники не смешивать: **«Статья»** (`analytic_articles`) — для «Приход/Расход»/кассы; **«Виды услуг»** (`services_type`) — для «Начислений»/счётчиков. Поле «Виды услуг» в денежных документах было введено и **отменено** (решение владельца): из «Приход/Расход» и «Регистра денежных средств» оно убрано и заменено на «Контрагент» (миграция `0008_cash_contractor`). Названия полей/колонок в UI приведены к именам справочников («Статья», «Контрагент»).
> - **Статьи доходов и расходов** — эталонный справочник задан (доходы: Поступления от жителей, Возвраты от контрагентов, Прочие доходы; расходы: Электроэнергия, Водоснабжение, Вывоз мусора и утилизация, Заработная плата персонала, Обслуживание инженерных систем, Благоустройство территории, Безопасность и проверки, Материалы и инвентарь, Возвраты жителям, Прочие расходы). Источник истины — `_ANALYTIC_ARTICLES` в `backend/init_data.py`; сидер `backend/seed_analytic_articles.py` добавляет недостающие строки (идемпотентно). В живой БД статьи внесены.
> - Переразнесение документов «Приход/Расход» на новые статьи **Выполнено** (миграция `migrate_expand_article_2026.py`, единственная транзакция): 778 операций-расходов с «Расходы по дому» разнесены по 9 статьям (сумма сходится 60 039 094, контроль печатается в скрипте). Проверено: ссылок на старую статью нет → статья «Расходы по дому» **удалена**; в справочнике осталось 13 строк (3 дохода + 10 расходов). «Статья» в регистрах (cash_register/accounts_register) не дублируется — она берётся из `transactions.article_id`, поэтому везде корректна автоматически.
> - Исторический перенос «Контрагентов» для существующих денежных операций и повторный перезалив истории — ОТЛОЖЕНО (по старым операциям контрагент не восстановим без повторного импорта). Новые документы «Приход/Расход» уже позволяют указывать «Контрагент».
> - Тип тарифа **«По жильцам»** — отложено (вернуться позже). Не вводить в `tariff_types` и логику расчёта начислений до отдельного решения (нужны: источник количества жильцов в квартире и поддержка типа в `calculate_accrual_for_account_service`).
> - **Prod-накат завершён (сент. 2026):** ветка `feat/owners-to-counterparties` + миграции до `0011` применены к прод-БД `townhouse`; таблица `owners`переименована в `counterparties`, добавлены тип «Входящий остаток» и `transactions.doc_no`. История перезалита по контроль-сборке со `townhouse_stage`: касса 10 140 (расход 60 039 094), начисления 10 179, квитанции 1 816 (сумма 60 008 492), показаний 1 813; прод-`users` (migration, admin) сохранены. Орфанов 0, последовательности пересчитаны. Бэкапы в `backend/prod_backup_*.sql` и `backend/prod_users_backup_*.sql`.

---

## 1. Технический долг (из первоначального анализа)

| # | Задача | Обоснование / как |
|---|--------|-------------------|
| 1.1 | Разбить `backend/app.py` (~3000 строк) на модули | ✅ Готово. `app.py` уменьшён с 3035 до ~680 строк. Вынесено: `field_config.py` (`MODEL_MAP`/`FIELD_CONFIG`/`coerce_field_value`), `serializers.py` (сериализаторы/`SERIALIZERS`), `services.py` (8 сервисных функций начислений/транзакций/регистров), `routers/{documents,registers,receipts,others}.py` (спец-эндпоинты). В `app.py` остались auth-роутер, `meta` и generic CRUD. pytest: 31/31 зелёные (см. 3.1). Полный smoke по всем группам — OK. |
| 1.2 | Убрать или оживить `schemas.py` | ✅ Pydantic-модели были неиспользуемы (API на голых `dict`) и устарели (нет новых сущностей/полей). Удалён как мёртвый код. Если при распиле `app.py` (п. 1.1) захочется перейти на типизированные ответы — вести их заново осознанно. |
| 1.3 | Аутентификация и закрыть CORS | ✅ Реализовано: модель `User` + роли, JWT-логин (`/api/auth/login`, `/api/auth/me`, `/api/auth/users`), PBKDF2-хеши, CORS через `CORS_ORIGINS` вместо `['*']`. См. секцию «Аутентификация и ролевой доступ». |
| 1.4 | Ввести миграции БД (Alembic) | ✅ Схема полностью под управлением Alembic. БД на head (`c1c2a44669d3`, после 0000_baseline → 0001_users → 0002_schema_squash → writeoff_documents); исторические ручные SQL-миграции (`cash_register`, `services_type.priority`) инкорпорированы в `0002_schema_squash`. Аудит `alembic revision --autogenerate` не выявляет ручных правок сверх моделей; единственный «дрейф» — тип `users.role` (VARCHAR↔Enum, косметический, native_enum=False), зафиксирован в версиях. |
| 1.5 | Серверная сортировка по вложенным полям | ✅ Общее решение (роадмап): новый `backend/sorting.py` вместо рукописных SQL-подзапросов. Поле описывается декларативно как путь по relationship (напр. `account.apartment.owner` → `full_name`), а коррелированный скалярный подзапрос строится автоматически через SQLAlchemy (`.correlate()` — корректно работает с eager-load одно-ко-многим и пагинацией). Поддержаны агрегаты (`readings_count`, `total_amount`, `items_count`, `total_allocated`), автор (`created_by_name` → COALESCE) и составные выражения (документ во взаиморасчётах). Прямые столбцы сортируются через `hasattr`. Ранее `created_by_name` у квитанций падал с 500 — теперь работает. pytest: `test_sorting.py`. |
| 1.7 | Проверить пересчёт балансов при массовых правках | ✅ Реализовано: `recalculate_account_balance`/`recalculate_register_balance` «с нуля» + `rebuild_accounts_register` (полная пересборка) + `check_register_integrity`. pytest: `test_payment_update_recalculates_cash_balance`, `test_rebuild_accounts_register_restores_consistency`, `test_rebuild_is_deterministic`. |
| 1.8 | Ни один документ не хранит автора/историю изменений | ✅ Реализовано (см. п. 2.9): аудит-поля (`created_by`/`updated_by`/`updated_at`/`change_description`) добавлены ко всем документам и проставляются при создании/изменении. |
| 1.9 | Название документов генерировать автоматически, чтобы пользователь не имел возможности вводить/редактировать его вручную (к примеру документ "Показания" текст должен быть примерно Показания за август 2026 г. | ✅ Реализовано: авто-названия и игнорирование ручного ввода — «Приход/Расход» (`build_transaction_title`/`set_transaction_title`: «Приход в кассу №17 от 24.08.2026»), начисления (`default_accrual_document_title`: «Начисление за август 2026»), показания (`build_meter_reading_document_title`: «Показания за август 2026 — Электричество»), списание (`build_writeoff_document_title`: «Списание задолженностей №5 от 27.08.2026»). Учитывается во всех путях создания/изменения (generic CRUD + спец-эндпоинты). |
| 1.10 | Дата документа не должна быть больше текущей, а также дата показаний не должна быть больше текущей даты | ✅ Реализовано: `validate_date_not_future` применяется к дате операции (Приход/Расход), дате показаний (документ и строка), дате начислений; период квитанций тоже ограничен текущим месяцем (`generate_receipts`). |

---

## 2. UX и функциональность

| # | Задача | Обоснование / как |
|---|--------|-------------------|
| 2.1 | Ресайз и перестановка колонок через библиотеку | Ручная реализация (HTML5 DnD) в AntD Table не заработала при `scroll={{x:'max-content'}}` и была удалена. Надёжно делается через `react-resizable` (ресайз) и `@dnd-kit` / `react-dnd` (перестановка). Требует добавления npm-зависимости. |
| 2.2 | Экспорт регистров и документов в Excel/CSV | Кнопки «Экспорт» в списках; выгрузка с учётом текущих фильтров/пагинации. |
| 2.3 | Отчёты по лицевым счетам | ⏳ Частично: реализован отчёт по одному счёту `GET /api/accounts/{id}/statement` (начислено / оплачено / долг по услугам, внесено / переплата — для отчёта и будущего ЛК). Осталось: сводка по дому / за период, настройка периода в UI. |
| 2.4 | Квитанции: создание, PDF, сохранение | ✅ Реализовано: модели `receipt_documents`/`receipt_items`, генерация по всем счетам за период, долг/переплата на «Фонд развития», PDF через `reportlab` (столбец «Переплата»), реализуем в UI (меню + ReceiptsModal). Осталось: выгрузка ZIP всех квитанций периода, при необходимости брендинг. |
| 2.5 | **Списание — отдельным документом** | ✅ Реализовано: модели `writeoff_documents`/`writeoff_items` + `writeoff_id` в `accounts_register` (каскадная отмена), миграция Alembic. `writeoffs.py`: `create_writeoff_document` (документ + строки + привязка) и `cancel_writeoff_document` (отмена + пересчёт балансов); ядро `calculate_write_offs` сохранено для rebuild/тестов. API: `POST /api/writeoff_documents/run` (заменил `write_offs/run`), `POST /api/writeoff_documents/{id}/cancel`, generic list. Фронт: меню «Списания», кнопка «Отменить» у активных документов. Автозапуск при «Приход/Расход» — через документ. |
| 2.6 | **Настраиваемый доступ к формам/меню (админ)** | Сейчас права ролей зашиты в коде (`permissions.py`, `can.ts`, `menuAccess.ts`). Сделать **форму настроек прав** для администратора, где можно включать/выключать доступ к разделам меню, формам и действиям (создание/изменение/удаление/чтение) по каждой роли. Хранение — таблица настроек доступа (например `role_permissions`), доступ к которой читается в рантайме вместо/в дополнение к зашитым правилам. |
| 2.7 | **Счетчик — только по тарифам «По счетчику»** | При создании счётчика (`meters`) разрешать выбор услуги только тех `services_type`, у которых тип тарифа «По счетчику» (иначе счётчик бессмыслен). Ограничение и на бэкенде (валидация при создании/изменении счётчика), и на фронте (фильтр выпадающего списка внешних ключей). |
| 2.8 | **Переделать обработчики «Выполнить списание»** | ✅ Готово вместе с п. 2.5: `WriteOffsModal` и автозапуск используют документный слой (`writeoff_documents/run`); результат по каждому счёту, без частичных записей. |
| 2.9 | **Аудит документов: автор и история изменений** | ✅ Реализовано (бэкенд): поля `created_by`/`updated_by`/`updated_at`/`change_description` добавлены к документам (Transaction, AccrualDocument, MeterReadingDocument, ReceiptDocument, WriteoffDocument), проставляются при создании/изменении (generic CRUD + спец-эндпоинты). Миграция Alembic `fce9ed483469`. В формы/журналы добавлен столбец «Автор» (`created_by_name`). `change_description` пока только для части эндпоинтов — полное «что изменилось» можно расширить. |
| 2.10 | **Настройка отображаемых столбцов на формах (по ролям)** | ✅ Вариант A (видимость колонок) реализован: кнопка «Колонки» в `GenericList` (чек-боксы по `columnsConfig`), сохранение в localStorage по (ресурс, роль). Колонки «ID»/«Действия» всегда видны; новые колонки по умолчанию видны. Осталось (по желанию): порядок и ширина колонок (Вариант C — нужны библиотеки dnd/resizable), либо серверное сохранение per-user (Вариант B). |
| 2.11 | **Отчёты: «Отчет по кассе», «Отчет по расходам», «Выписка по счету», «Отчет по должникам»** | 🔜 Частично. **(а) Отчёт по кассе** ✅ Реализован: `GET /api/reports/cash_register` (остаток на начало / приход / расход / остаток на конец по кассам + движение по документам с аналитикой), меню «Отчеты → По кассе». **Доработка «Приход/Расход»**: поле «Квартира» стало необязательным (операции без квартиры идут в общий `cash_register` с `account_id=NULL`, видны в отчёте по кассе), добавлен справочник **«Аналитика»** (`analytic_articles`, единый: статьи доходов/расходов, `transactions.article_id`) — для отчётов по статьям. **UI справочников** (`ReferenceSelect`): показываются **все** записи (а не первые 10 — обход Refine-пагинации через `authedFetch`), а для **необязательных** полей (напр. «Квартира») в списке есть пункт **«— пусто —»**, чтобы очистить значение выбором из списка (а не крестиком) — операция снова может быть «без квартиры» после того, как выбрали. **(б) Отчёт по расходам** ✅ Реализован: `GET /api/reports/expenses` (расход кассы за период: итог, по статьям расходов, детализация по документам/контрагентам), фронт `ExpenseReport.tsx`, меню «Отчеты → По расходам». **(в) Выписка по счёту** ✅ Реализован: `GET /api/reports/statement?account_id=…` (помесячно начислено/списано/остаток), фронт `StatementReport.tsx`. **(г) Отчёт по должникам** ✅ Реализован: `GET /api/reports/debtors` (активные л/с с долгом, по убыванию), фронт `DebtorsReport.tsx`. |
| 2.11 | **Сортировка** | В форме "Приход/Расход" колонки Аналитика и "Автор" без возможности сортировки.
---

## 3. Качество и надёжность

| # | Задача | Обоснование / как |
|---|--------|-------------------|
| 3.1 | Тесты бэкенда (pytest) | ✅ Реализовано: **все 31 тест зелёный (31/31)**. `test_accrual_formulas` и `test_writeoffs` переведены на самодостаточные услуги (создают свои `ServiceType`/`TariffType`/`Tariff` вместо жёстких ID 6/7) — больше не зависят от наполнения справочника. Расчёт начислений/списание, аутентификация + ролевой доступ покрыты. Не покрыты: CRUD generic-ресурсов (справочников), PDF-генерация квитанций. |
| 3.2 | Тесты фронтенда (vitest + RTL) | ⏳ Не начато: ключевые сценарии GenericList (рендер, сортировка, модалки). |
| 3.3 | CI (при желании) | ⏳ Не настроено: прогон `tsc`, `build`, pytest при пуше. |
| 3.4 | Проверка и чистка накопленных данных | ⏳ Частично: `check_register_integrity` + pytest для восстановления среза; аудит реальных данных (все счета / корректность названий документов) не проводился. |

---

## 4. Миграция данных из старой БД

Перенос исторических данных из электронной таблицы (`templates/Миграция данных FTH.ods`, аналог `МиграцияДанных.xlsx`) в новую БД. В источнике НЕТ документов — только регистровые записи (Касса-Приход/Расход, Начисления-…, Показания, Тарифы, Счетчики), поэтому документы нужно синтезировать.

| # | Задача | Обоснование / как |
|---|--------|-------------------|
| 4.1 | **Очистка данных в электронной таблице** | Исходник содержит мусор: итоговые строки («итого», большие суммы при пустой квартире), нулевые суммы, битые даты (напр. 1900 г., serial 42), «битые» коды квартир (`0`, `24`, `35`, `1307194`) и несуществующая «Квартира 18» (общедомовые операции). Батчно: парсер .ods, контрольные суммы прихода/расхода/начислений «в» vs «из» таблицы, очистка/отбрасывание с отчётом. |
| 4.2 | **Приведение к единообразию справочников** | Привести справочники источника к целевой модели. Создать ДВА отдельных справочника: **«Виды задолженностей»** — для регистра «Начисления» (`accruals_register`), и **«Аналитика»** — для регистра «Взаиморасчеты» (`accounts_register`)/«Приход/Расход». В источнике эти понятия были слиты в один «Аналитики/Виды задолженностей» (7 шт.: Электроэнергия, Охрана, Электричество охраны, Обслуживание ТП, Вывоз мусора, Фонд развития, Прочие расходы). Разнести по типам (в т.ч. `kind` доход/расход для Аналитики), стол `Статья` (Коммунальные/Эксплуатационные) — либо убрать, либо учесть как дополнительную группировку. Привести Квартиры/Контрагенты/Счетчики/Тарифы к справочникам проекта (`Apartment`/`Owner`/`Account`, `Meter`, `Tariff`, `ServiceType`). |
| 4.3 | **Синтез документов из записей регистров** | Документов в источнике нет. Показания и начисления сгруппировать по (период, услуга) и создать документы-шапки (`MeterReadingDocument`, `AccrualDocument`) со строками; каждый приход/расход «Кассы» — отдельный документ `Transaction` (Приход/Расход) с автогенерируемым названием. Производный `accounts_register`/балансы — НЕ тащить, а пересобрать силами проекта (`rebuild_accounts_register`/`check_register_integrity`). |
| 4.4 | **Генерация значений обязательных полей** | Для отсутствующих обязательных атрибутов сформировать значения по правилам: лицевой счёт на каждую квартиру (`Account`), `cash_point` (Касса/Банк), `service_type` по «Виду задолженности», тариф по историческому тарифу строки, `article_id`/`kind` для приходов (см. 4.2), автор (`created_by`) служебным миграционным пользователем, даты по факту и т.п. |

---

## Бэклог доработок (после наката на прод)

Заявки оператора/заказчика. Не закрыты — по мере приоритизации. Порядок колонок/форм и локализация уточняются при реализации.

| # | Задача | Домен / где | Детали |
|---|--------|-----------|-------------------------|
| Б1 | Тип начисления из «Месячные» → «Регулярные» | Документ «Начисления» | Переименовать лейбл типа начисления (`doc_kind`: сейчас `monthly` отображается как «Месячные» → показывать «Регулярные»). |
| Б2 | ЛК жителя: редактирование данных собственника | Личный кабинет | Возможность менять контактные данные владельца (телефон, email и т.д.) из личного кабинета (с ограничениями по правам). |
| Б3 | Префикс лицевых счетов задаёт админ | Администрирование | Настраиваемый префикс генерации `account_number` (сейчас «LS/NNNN»). Несрочно. |
| Б4 | «Регистр начислений»: колонка «Тариф» | Регистр начислений | Показывать тариф по строке начисления. |
| Б5 | Документ «Начисления»: «Тип» и «Примечание» не сортируются | Документ «Начисления» | Добавить серверную сортировку по полям «Тип» (`doc_kind`) и «Примечание». |
| Б6 | «Показания»/«Квитанции»: нет поля «Примечание» | Документы | Добавить колонку «Примечание» в список и форму элемента документов «Показания» и «Квитанции». |
| Б7 | Кнопки с иконками вместо текстовых | UI (по всем формам) | Заменить текстовые кнопки на иконки (с `tooltip`). |
| Б8 | Тип тарифа «По жильцам» | Справочник «Типы тарифов» | Добавить тип тарифа «По жильцам» (и поддержку расчёта от числа жильцов, см. отложенный п.). |
| Б9 | Пароль ЛК «fth»+номер л/с; логин аналогично; смена пароля при первом входе | ЛК жителя / auth | По умолчанию логин и пароль для residents: `fth` + номер лицевого счёта; принудительная смена пароля при первом входе. |
| Б10 | Фильтрация данных на формах | Generic-списки | Добавить фильтры по колонкам (общий механизм на формах). |
| Б11 | Тарифы: просмотр «актуальных» + статус/фильтр | Справочник «Тарифы» | Показывать действующие тарифы; возможно добавить статус тарифа и фильтр по статусу. |
| Б12 | Удалить «мусорные» контрагентов и квартиры | Справочники | Почистить записи-артефакты (тестовые/битые л/с, контрагенты) — с подтверждением. |
| Б13 | «Регистр начислений»: нет прошлых/текущих показаний | Регистр начислений | Вернуть колонки «Показание прошлое/текущее» (сейчас только потребление) + добавить «Тариф», по которому было начисление. |
| Б14 | Прод: ID таблиц с 1 и чистая нумерация | Прод-БД | Пересоздать прод-БД так, чтобы ID всех таблиц начинались с 1, без «дырок» в счётчиках (новые записи получают id = max+1). (На dev этот порядок уже выполнен сценариями пере-импорта/перенумерации.) |

---

---

## Приоритеты (предлагаемый порядок)

1. ~~1.4 Alembic~~ ✅ · ~~1.2 schemas.py~~ ✅ · ~~1.1 Распил app.py~~ ✅ — технический долг закрыт.
2. ~~2.5/2.8 Списание отдельным документом~~ ✅ — реализовано (документ + отмена + журнал).
3. **2.9 Аудит документов** (автор/история изменений) — логичное продолжение: новые документы списания уже имеют `created_by`; перенести метаданные автора на остальные документы (начисления, показания, квитанции, Приход/Расход).
4. **2.7 Счётчик только по «По счетчику»** — небольшая, быстро проверяется на текущих данных.
5. **ЛК жителя** ✅ — реализовано (привязка `User.account_id` + сводка + свои квитанции). Дальше (по желанию): оплата онлайн, показания из ЛК, история начислений/платежей.
6. **2.11 Отчёты («По кассе», «По расходам», «Выписка по счёту», «По должникам»)** — управленческий слой; период + экспорт. Логично после стабилизации регистров.
7. ~~4. Миграция данных из старой БД~~ ✅ — перенос истории (4.1..4.4) реализован и проверен на чистой БД: подготовка `templates/clean` (`migrate_prepare_sources.py`) + синтез `migrate_import.py`/`migrate_synthetic/*` (справочники, тарифы, показания, начисления, входящие-старт, касса, пересборка). Порядок запуска — `backend/migrations/README.md`.
8. Остальное (UX 2.x: 2.1 ресайз, 2.2 экспорт, 2.3 отчёты по дому, 2.6 настраиваемые права, 2.4 квитанции ZIP) — по мере необходимости.

---

## Статус уже сделанного (справочно)

- ✅ **П. 1.4 Alembic — схема под контролем**: БД на head (`c1c2a44669d3`), исторические ручные SQL-миграции инкорпорированы в `0002_schema_squash`. `alembic revision --autogenerate` не выявляет ручных правок сверх моделей; весь «дрейф» сводится к типу `users.role` (VARCHAR↔Enum, косметический).
- ✅ **П. 1.2 — удалён мёртвый `schemas.py`**: Pydantic-модели не использовались и устарели (нет новых сущностей/полей), API работает на `dict` через `FIELD_CONFIG`/сериализаторы. Удалено.
- ✅ **П. 1.1 — распил `app.py`**: `app.py` с 3035 строк сокращён до ~680. Вынесены `field_config.py`, `serializers.py`, `services.py`, `routers/{documents,registers,receipts,others}.py`. `app.py` содержит только auth, `/meta` и generic CRUD. pytest 31/31, полный smoke по всем группам эндпоинтов — OK.
- ✅ **П. 2.5/2.8 — списание отдельным документом**: модели `writeoff_documents`/`writeoff_items` + `writeoff_id` в `accounts_register`; `create_writeoff_document`/`cancel_writeoff_document`; API `run`/`cancel`/`items`; меню и журнал «Списания» с просмотром и отменой; кнопка «Выполнить списание» перенесена в журнал; автозапуск при «Приход/Расход» — через документ.
- ✅ Целостность `accounts_register`: пересчёт балансов «с нуля» по истории.
- ✅ **Пересчёт регистров с нуля + идемпотентность**: обобщён `recalculate_register_balance` (для `accounts_register`/`cash_register`); добавлены `rebuild_accounts_register` (полная пересборка производного среза из `accruals_register`+`cash_register`) и `check_register_integrity` (аудит согласованности) + эндпоинт `POST /api/maintenance/rebuild_registers`. pytest покрывает восстановление испорченного среза и детерминизм повторного пересбора.
- ✅ **Формальный «переворот» знаков**: начисления в `income`, списание/оплата в `expense`; `balance_after>0`=долг, `<0`=переплата. Квитанции и отчёт считают долг/переплату из регистров (`_account_debt_overpayment`), переплата видна в отчёте (`overpayment`) и в `cash_register`. Скрипт миграции истории `migrations/flip_accounts_register_signs.py`.
- ✅ «Вид услуги» в регистре взаиморасчётов + сортировка по «№ квартиры» и «Документ».
- ✅ «№ квартиры» в двух регистрах; корректная серверная сортировка по квартире.
- ✅ Поля «Дата» и «Название» в документе «Приход/Расход» с авто-пересчётом.
- ✅ Убран столбец «Действия» в регистрах; единый порядок колонок в похожих формах.
- ✅ Тесты формул начислений по типу тарифа (Фиксированный / По площади / По счетчику) — `test_accrual_formulas.py`.
- ✅ **UI справочников (`ReferenceSelect`)**: показываются все записи (не только первые 10 — обход Refine-пагинации через `authedFetch`); для необязательных полей (квартира) в списке есть пункт «— пусто —», чтобы очистить значение выбором из списка (операция снова может быть «без квартиры»), а не только крестиком.
- ❌ Ресайз/перестановка колонок — отменено (не работало; см. 2.1).

### ✅ Операция «Списание задолженностей» (Фазы 0–3)
- ✅ Конвенция знаков зафиксирована комментариями в `models.py`/`app.py` (блок «КОНВЕНЦИЯ ЗНАКОВ»).
- ✅ Регистр денежных средств `cash_register` (миграция `0001`): «Приход/Расход» пишет сюда, а не во взаиморасчёты.
- ✅ `services_type.priority` (миграция `0002`) — приоритет списания услуги.
- ✅ Ядро `backend/writeoffs.py` `calculate_write_offs` — распределение денег по приоритету, идемпотентно, переплата висит отрицательным остатком (вариант 1).
- ✅ **Документный слой списания (п. 2.5/2.8)**: `create_writeoff_document`/`cancel_writeoff_document`, журнал «Списания», кнопка «Выполнить списание» в журнале (вместо устаревшей страницы «Приход/Расход»); `write_offs/run` заменён на `writeoff_documents/run`.
- ✅ Кнопка «Выполнить списание» (журнал «Списания», `WriteOffsModal`).
- ✅ Автозапуск списания при создании документа «Приход/Расход» (через документ).
- ✅ Отчёт по лицевому счёту `GET /api/accounts/{id}/statement`: начислено / оплачено / долг по услугам, внесено / переплата (для отчёта и будущего ЛК).
- ✅ pytest: тесты в `backend/tests/test_writeoffs.py` (регистр денежных средств, приоритет списания, идемпотентность, переплата, отчёт, пересборка среза) — зафиксирована логика Фаз 1–4.
- ✅ Скрипт миграции исторических данных `backend/migrations/migrate_old_payments_to_cash_register.py` (перенос старых оплат из `accounts_register` в `cash_register` с защитой от дублей).

### ✅ Аутентификация и ролевой доступ
- ✅ Пункт 1.3: модель `User` + роль (5 ролей: admin/operator/cashier/controller/resident), миграция Alembic `0001_users` (legacy `users` пустая — пересоздана чисто).
- ✅ JWT-логин: `POST /api/auth/login`, `GET /api/auth/me`, `POST /api/auth/users` (только admin). Хеш паролей PBKDF2-SHA256 (stdlib).
- ✅ Ролевой доступ: `permissions.py` (generic CRUD по (метод, ресурс)) + защита спец-эндпоинтов (начисление/списание/пересбор/отчёт).
- ✅ CORS: явные origins из `CORS_ORIGINS` (по умолчанию `townhouse.sagacloud.kz` + localhost), вместо `['*']`.
- ✅ CLI: `backend/create_user.py` для первоначального заведения админа.
- ✅ pytest: тесты auth (хеш, JWT, login, все роли) + HTTP-тесты ролевого доступа через TestClient (httpx).
- ✅ **Личный кабинет жителя (ЛК)**: привязка `User.account_id` (миграция `0003_resident_account_link`), эндпоинты `GET /api/me/statement` (сводка по своему счёту) и `GET /api/me/receipts` (свои квитанции), ограничение `resident` только своим счётом/квитанциями (`_ensure_can_view_account`, `_raise_for_resident_other` для statement/items/pdf). Фронт: страница `ResidentCabinet.tsx` (сводка + детализация по услугам + список квитанций с просмотром и PDF), меню «Мой кабинет» — только роль `resident`. Seed привязывает `resident` к `FTH-001`.
